---
phase: 01-code-quality-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - astro.config.mjs
  - eslint.config.mjs
  - .prettierrc
  - .prettierignore
  - vitest.config.ts
autonomous: true
requirements: [QUAL-02, QUAL-03, QUAL-04, QUAL-06]

must_haves:
  truths:
    - "npm run lint executes ESLint 9 flat config with typescript-eslint strict and eslint-plugin-astro"
    - "npm run format:check runs Prettier with astro plugin"
    - "npm run typecheck runs tsc --noEmit"
    - "npm run test executes Vitest with happy-dom environment"
    - "Import paths using @/ resolve to src/ in all tools"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "ESLint 9 flat config"
      contains: "tseslint.configs.strict"
    - path: ".prettierrc"
      provides: "Prettier configuration"
      contains: "prettier-plugin-astro"
    - path: "vitest.config.ts"
      provides: "Vitest config with happy-dom and coverage"
      contains: "happy-dom"
    - path: "tsconfig.json"
      provides: "Path alias and strict mode"
      contains: "@/*"
  key_links:
    - from: "eslint.config.mjs"
      to: "typescript-eslint"
      via: "import tseslint"
      pattern: "tseslint\\.config"
    - from: "vitest.config.ts"
      to: "src/"
      via: "resolve alias"
      pattern: "'@'.*resolve.*'./src'"
---

<objective>
Install and configure the complete code quality toolchain: ESLint 9, Prettier, Vitest, and TypeScript path aliases.

Purpose: Establish the foundation that all subsequent plans depend on -- linting, formatting, testing, and type checking must work before scripts can be extracted and tested.
Output: Configuration files and npm scripts for lint, format, typecheck, and test commands.
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-quality-tooling/01-CONTEXT.md
@.planning/phases/01-code-quality-tooling/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dev dependencies and add npm scripts</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state: no dev dependencies, only dev/build/preview scripts)
    - .planning/phases/01-code-quality-tooling/01-RESEARCH.md (exact package versions and script definitions)
  </read_first>
  <action>
Install all dev dependencies in a single command:

```bash
npm install -D eslint@^9.0.0 @eslint/js typescript-eslint eslint-plugin-astro eslint-plugin-jsx-a11y eslint-config-prettier globals prettier prettier-plugin-astro vitest @vitest/coverage-v8 happy-dom husky lint-staged
```

NOTE: Pin eslint to ^9.0.0 (not ^10) per research recommendation -- eslint-plugin-astro 1.6.0 has verified ESLint 9 flat config support but ESLint 10 compat is unverified.

Add the following scripts to package.json (merge with existing dev/build/preview):

```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepare": "husky"
  }
}
```

Add lint-staged configuration to package.json:

```json
{
  "lint-staged": {
    "*.{ts,mjs,js}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.astro": [
      "eslint --fix",
      "prettier --write --plugin=prettier-plugin-astro"
    ],
    "*.{css,json,md,mdx}": [
      "prettier --write"
    ]
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/aristofanischionis/Public/Projects/imizicyber.github.io && node -e "const p=require('./package.json'); const s=p.scripts; const d=p.devDependencies; console.log('lint:', !!s.lint, 'format:check:', !!s['format:check'], 'typecheck:', !!s.typecheck, 'test:', !!s.test, 'prepare:', !!s.prepare, 'eslint:', !!d.eslint, 'vitest:', !!d.vitest, 'prettier:', !!d.prettier, 'husky:', !!d.husky, 'lint-staged:', !!p['lint-staged'])"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains script "lint" with value "eslint src/"
    - package.json contains script "format:check" with value "prettier --check ."
    - package.json contains script "typecheck" with value "tsc --noEmit"
    - package.json contains script "test" with value "vitest run"
    - package.json contains script "prepare" with value "husky"
    - package.json devDependencies contains eslint, typescript-eslint, eslint-plugin-astro, eslint-plugin-jsx-a11y, eslint-config-prettier, globals
    - package.json devDependencies contains prettier, prettier-plugin-astro
    - package.json devDependencies contains vitest, @vitest/coverage-v8, happy-dom
    - package.json devDependencies contains husky, lint-staged
    - package.json contains lint-staged key with entries for *.astro and *.ts
    - `npm ls eslint` shows eslint@9.x (not 10.x)
  </acceptance_criteria>
  <done>All dev dependencies installed, all npm scripts defined, lint-staged config in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Create ESLint, Prettier, Vitest configs and path alias</name>
  <files>eslint.config.mjs, .prettierrc, .prettierignore, vitest.config.ts, tsconfig.json, astro.config.mjs</files>
  <read_first>
    - tsconfig.json (current: only extends astro/tsconfigs/strict, needs baseUrl and paths)
    - astro.config.mjs (current: site, output, trailingSlash, build, integrations -- needs vite.resolve.alias added)
    - .planning/phases/01-code-quality-tooling/01-RESEARCH.md (exact config contents for all files)
  </read_first>
  <action>
Create `eslint.config.mjs` at project root:

```javascript
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';
import eslintPluginAstro from 'eslint-plugin-astro';
import eslintConfigPrettier from 'eslint-config-prettier';
import globals from 'globals';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.strict,
  ...eslintPluginAstro.configs.recommended,
  ...eslintPluginAstro.configs['jsx-a11y-recommended'],
  eslintConfigPrettier,
  {
    languageOptions: {
      globals: {
        ...globals.browser,
        ...globals.node,
      },
    },
  },
  {
    ignores: ['dist/', '.astro/', 'node_modules/'],
  },
);
```

Create `.prettierrc` at project root:

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "all",
  "printWidth": 100,
  "plugins": ["prettier-plugin-astro"],
  "overrides": [
    {
      "files": "*.astro",
      "options": {
        "parser": "astro"
      }
    }
  ]
}
```

Create `.prettierignore` at project root:

```
dist/
.astro/
node_modules/
package-lock.json
*.min.js
*.min.css
```

Create `vitest.config.ts` at project root:

```typescript
import { defineConfig } from 'vitest/config';
import { resolve } from 'path';

export default defineConfig({
  resolve: {
    alias: {
      '@': resolve(__dirname, './src'),
    },
  },
  test: {
    environment: 'happy-dom',
    include: ['src/**/*.test.ts', 'src/__tests__/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      include: ['src/scripts/**/*.ts'],
      exclude: ['src/scripts/**/index.ts'],
      thresholds: {
        statements: 80,
        branches: 80,
        functions: 80,
        lines: 80,
      },
    },
  },
});
```

Update `tsconfig.json` to add path alias (keep existing extends):

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

Update `astro.config.mjs` to add vite resolve alias. Add the import for `resolve` from `path` at the top, and add the `vite` key to the config object. Keep all existing config (site, output, trailingSlash, build, integrations) exactly as is:

```javascript
import { defineConfig } from 'astro/config';
import sitemap from '@astrojs/sitemap';
import mdx from '@astrojs/mdx';
import { resolve } from 'path';

export default defineConfig({
  site: 'https://imizicyber.com',
  output: 'static',
  trailingSlash: 'always',
  build: {
    format: 'directory',
  },
  integrations: [
    sitemap({
      filter: (page) => !page.includes('/company-profile/'),
    }),
    mdx(),
  ],
  vite: {
    resolve: {
      alias: {
        '@': resolve('./src'),
      },
    },
  },
});
```
  </action>
  <verify>
    <automated>cd /Users/aristofanischionis/Public/Projects/imizicyber.github.io && test -f eslint.config.mjs && test -f .prettierrc && test -f .prettierignore && test -f vitest.config.ts && node -e "const t=require('./tsconfig.json'); console.log('paths:', JSON.stringify(t.compilerOptions?.paths))" && echo "ALL CONFIG FILES EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.mjs exists and contains `import tseslint from 'typescript-eslint'`
    - eslint.config.mjs contains `tseslint.configs.strict`
    - eslint.config.mjs contains `eslintPluginAstro.configs.recommended`
    - eslint.config.mjs contains `eslintPluginAstro.configs['jsx-a11y-recommended']`
    - eslint.config.mjs contains `eslintConfigPrettier`
    - eslint.config.mjs ignores array includes `'dist/'`, `'.astro/'`, `'node_modules/'`
    - .prettierrc contains `"prettier-plugin-astro"` in plugins array
    - .prettierrc contains `"parser": "astro"` override for *.astro files
    - .prettierignore contains `dist/` and `.astro/`
    - vitest.config.ts contains `environment: 'happy-dom'`
    - vitest.config.ts contains coverage thresholds of 80 for statements, branches, functions, lines
    - vitest.config.ts contains `'@': resolve(__dirname, './src')`
    - tsconfig.json contains `"@/*": ["src/*"]` in compilerOptions.paths
    - tsconfig.json contains `"baseUrl": "."`
    - astro.config.mjs contains `'@': resolve('./src')` in vite.resolve.alias
    - `npx eslint --print-config src/data/site.ts` exits without error
    - `npx prettier --check .prettierrc` exits 0
  </acceptance_criteria>
  <done>ESLint 9 flat config, Prettier, Vitest, and path alias all configured and loadable</done>
</task>

</tasks>

<verification>
- `npm run lint` executes (may show violations -- that is expected, fixing comes later)
- `npm run format:check` executes (may show unformatted files -- expected)
- `npm run typecheck` executes (may show type errors -- expected)
- `npm run test` executes with "no test files found" (tests created in Plan 03)
- `npx eslint --print-config src/data/site.ts` outputs rule configuration without errors
</verification>

<success_criteria>
All four toolchain commands (lint, format:check, typecheck, test) execute without configuration errors. Config files are syntactically valid and loadable by their respective tools. Path alias @/ resolves to src/ in tsconfig, astro, and vitest configs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-quality-tooling/01-01-SUMMARY.md`
</output>
