---
phase: 08-blog-system-and-new-content
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - src/components/Nav.astro
  - src/components/sections/ServicesSection.astro
autonomous: true
requirements: [SRVC-11]

must_haves:
  truths:
    - 'USSD/mobile money security testing is accessible from the services navigation'
    - 'A visitor can navigate from the services area to USSD/mobile money content'
  artifacts:
    - path: 'src/components/Nav.astro'
      provides: 'Services dropdown or link including USSD/mobile money content'
      contains: "ussd\\|mobile-money\\|mobile.money"
  key_links:
    - from: 'src/components/Nav.astro'
      to: '/blog/mobile-money-security-testing/'
      via: 'Navigation link or dropdown'
      pattern: 'mobile-money-security-testing'
---

<objective>
Link USSD and mobile money security testing content from the services navigation so visitors can discover this Africa-specific offering. Verify the complete phase by building and checking all 5 success criteria.

Purpose: USSD/mobile money security testing is a key differentiator for an East Africa-focused cybersecurity firm. Making it discoverable from services navigation connects this content to the buyer journey.
Output: Navigation updated, full phase verification complete.
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-blog-system-and-new-content/08-01-SUMMARY.md
@.planning/phases/08-blog-system-and-new-content/08-02-SUMMARY.md

@src/components/Nav.astro
@src/components/sections/ServicesSection.astro

<interfaces>
From src/components/Nav.astro:
```typescript
interface Props {
  variant?: 'home' | 'inner';
  showFreeTool?: boolean;
}
// Services link: `<a href={servicesHref}>Services</a>` where servicesHref is '#services' or '/#services'
// Blog link: `<a href={blogHref}>Blog</a>`
```

Existing blog posts for USSD/mobile money:

- /blog/ussd-security-testing/ — "Why USSD banking services in Rwanda need security testing"
- /blog/mobile-money-security-testing/ — "Mobile money security testing: MoMo, M-Pesa and USSD platforms"
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add USSD/mobile money link to services navigation and services section</name>
  <files>src/components/Nav.astro, src/components/sections/ServicesSection.astro</files>
  <action>
The goal is to make USSD/mobile money security testing discoverable from the services area. There are two existing comprehensive blog posts covering this topic. The approach depends on the current Nav structure:

**Option A (preferred — add to ServicesSection):**
If the homepage ServicesSection already lists service offerings in a grid or list format, add a "Mobile Money & USSD Security" item that links to `/blog/mobile-money-security-testing/`. This positions it alongside other services without requiring a full service page. Use the same card/grid styling as existing service items. Brief description: "Security testing for MoMo, M-Pesa, Airtel Money, and USSD banking platforms across East Africa."

**Option B (Nav dropdown):**
If the Nav component supports or can easily support a dropdown for Services, add "Mobile & USSD Security" as a dropdown item linking to `/blog/mobile-money-security-testing/`. If the Nav uses a simple single link to `#services` or `/#services`, do NOT create a complex dropdown — use Option A instead.

**Implementation steps:**

1. Read `src/components/sections/ServicesSection.astro` to understand its current structure
2. Add a "Mobile Money & USSD Security" entry to the services grid/list that links to `/blog/mobile-money-security-testing/`
3. Style it consistently with the existing service entries
4. If the ServicesSection has a data-driven approach (array of services), add the new item to the array
5. If it's hardcoded HTML, add a new card/entry matching the pattern

The link MUST be `/blog/mobile-money-security-testing/` (the more comprehensive of the two existing posts). The entry should make it clear this is a specialised testing offering, not just a blog post.

Do NOT create a new service page for this — the blog post already serves as comprehensive content. The requirement says "A page or blog article covering USSD and mobile money security testing exists and is linked from the services navigation" — the blog article exists, we just need the navigation link.
</action>
<verify>
<automated>cd /Users/aristofanischionis/Public/Projects/imizicyber.github.io && npm run build 2>&1 | tail -5 && grep -r "mobile-money-security-testing" dist/index.html 2>/dev/null | head -3 || grep -r "mobile-money-security-testing" src/components/sections/ServicesSection.astro src/components/Nav.astro 2>/dev/null | head -3</automated>
</verify>
<done>USSD/mobile money security testing is linked from the services section on the homepage. A visitor browsing services can discover and navigate to the mobile money security testing content. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase verification — all 5 success criteria</name>
  <files>(no files modified — verification only)</files>
  <action>
Run a comprehensive verification of all Phase 8 success criteria:

1. **Tag filters on blog index:** Check that the built blog index page contains tag filter buttons and data-tags attributes.

   ```bash
   grep -c "data-tag" dist/blog/index.html
   grep -c "data-tags" dist/blog/index.html
   ```

2. **Related posts on blog post pages:** Check that a built blog post page contains a "Related articles" section.

   ```bash
   grep -c "Related articles\|related-posts" dist/blog/penetration-testing-rwanda/index.html
   ```

3. **Read time visible:** Check blog index cards show read time and blog posts show read time.

   ```bash
   grep -c "MIN READ" dist/blog/index.html
   grep -c "MIN READ" dist/blog/penetration-testing-rwanda/index.html
   ```

4. **3 new articles live:** Verify all 3 new blog posts were built.

   ```bash
   ls dist/blog/penetration-testing-kigali-enterprise-guide/index.html
   ls dist/blog/bnr-cybersecurity-compliance-2026/index.html
   ls dist/blog/cloud-security-east-africa-aws-azure/index.html
   ```

5. **USSD/mobile money linked from services:** Check homepage services section links to mobile money content.
   ```bash
   grep "mobile-money-security-testing" dist/index.html
   ```

If any check fails, investigate and fix the issue. Report results for all 5 criteria.
</action>
<verify>
<automated>cd /Users/aristofanischionis/Public/Projects/imizicyber.github.io && npm run build 2>&1 | tail -3 && echo "--- Check 1: Tag filters ---" && grep -c "data-tag" dist/blog/index.html && echo "--- Check 2: Related posts ---" && grep -c "Related" dist/blog/penetration-testing-rwanda/index.html && echo "--- Check 3: Read time ---" && grep -c "MIN READ" dist/blog/index.html && echo "--- Check 4: New articles ---" && ls dist/blog/penetration-testing-kigali-enterprise-guide/index.html dist/blog/bnr-cybersecurity-compliance-2026/index.html dist/blog/cloud-security-east-africa-aws-azure/index.html && echo "--- Check 5: USSD nav ---" && grep -c "mobile-money" dist/index.html && echo "ALL CHECKS PASSED"</automated>
</verify>
<done>All 5 phase success criteria verified: (1) tag filters on blog index, (2) related posts on blog post pages, (3) read time visible, (4) 3 new articles live, (5) USSD/mobile money linked from services navigation.</done>
</task>

</tasks>

<verification>
- Full build succeeds with 19 blog posts
- All 5 phase success criteria pass the automated checks described in Task 2
- No broken links in the build output
</verification>

<success_criteria>

- USSD/mobile money security testing is accessible from services navigation on homepage
- All 5 phase success criteria verified in the built output
- Build passes cleanly with zero errors
  </success_criteria>

<output>
After completion, create `.planning/phases/08-blog-system-and-new-content/08-03-SUMMARY.md`
</output>
