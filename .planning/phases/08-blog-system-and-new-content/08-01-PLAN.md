---
phase: 08-blog-system-and-new-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/content/blog/api-security-banking.mdx
  - src/content/blog/bank-fraud-east-africa-cybersecurity-lessons.mdx
  - src/content/blog/bnr-cybersecurity-requirements.mdx
  - src/content/blog/data-breach-cost-east-africa.mdx
  - src/content/blog/east-africa-cybersecurity.mdx
  - src/content/blog/iso-27001-rwanda.mdx
  - src/content/blog/mobile-banking-security-assessment.mdx
  - src/content/blog/mobile-money-security-testing.mdx
  - src/content/blog/penetration-testing-cost-rwanda.mdx
  - src/content/blog/penetration-testing-rwanda.mdx
  - src/content/blog/penetration-testing-vs-vulnerability-assessment-banks.mdx
  - src/content/blog/penetration-testing-vs-vulnerability-scanning.mdx
  - src/content/blog/social-engineering-east-africa-banks.mdx
  - src/content/blog/swift-csp-compliance-rwanda.mdx
  - src/content/blog/ussd-security-testing.mdx
  - src/content/blog/vapt-rwanda.mdx
  - src/pages/blog/index.astro
  - src/layouts/BlogPostLayout.astro
  - src/components/RelatedPosts.astro
  - src/components/TagFilter.astro
autonomous: true
requirements: [SRVC-07, SRVC-08, SRVC-10]

must_haves:
  truths:
    - 'Visitor can click tag buttons on the blog index to filter posts by category'
    - 'Each blog post page shows 2-3 related posts based on shared tags'
    - 'Read time is visible on every blog index card and at the top of every blog post'
  artifacts:
    - path: 'src/content.config.ts'
      provides: 'tags field in blog schema'
      contains: 'tags: z.array'
    - path: 'src/components/TagFilter.astro'
      provides: 'Client-side tag filter buttons'
      min_lines: 20
    - path: 'src/components/RelatedPosts.astro'
      provides: 'Related posts grid based on shared tags'
      min_lines: 25
    - path: 'src/pages/blog/index.astro'
      provides: 'Blog index with tag filtering and read time'
      contains: 'TagFilter'
    - path: 'src/layouts/BlogPostLayout.astro'
      provides: 'Blog post layout with related posts section'
      contains: 'RelatedPosts'
  key_links:
    - from: 'src/content.config.ts'
      to: 'src/content/blog/*.mdx'
      via: 'Zod schema validation'
      pattern: "tags: z\\.array"
    - from: 'src/pages/blog/index.astro'
      to: 'src/components/TagFilter.astro'
      via: 'Component import and tag data prop'
      pattern: 'TagFilter'
    - from: 'src/layouts/BlogPostLayout.astro'
      to: 'src/components/RelatedPosts.astro'
      via: 'Component import with current post tags'
      pattern: 'RelatedPosts'
---

<objective>
Add tag-based filtering to the blog index, a related posts section to every blog post, and ensure read time is displayed everywhere. This transforms the blog from a flat list into a navigable, interconnected content system.

Purpose: Visitors can discover relevant content by topic, and every blog post surfaces related articles — keeping readers engaged and improving SEO through internal linking.
Output: Tag filtering on blog index, RelatedPosts component on every post, tags added to all 16 existing blog posts.
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/content.config.ts
@src/pages/blog/index.astro
@src/layouts/BlogPostLayout.astro
@src/components/BlogCard.astro

<interfaces>
From src/content.config.ts:
```typescript
const blog = defineCollection({
  loader: glob({ pattern: '**/*.mdx', base: './src/content/blog' }),
  schema: z.object({
    title: z.string(),
    headline: z.string(),
    description: z.string(),
    keywords: z.string().optional(),
    datePublished: z.string(),
    dateModified: z.string().optional(),
    readTime: z.string().optional(),
    metaLabel: z.string().optional(),
    breadcrumbName: z.string(),
    canonicalUrl: z.string(),
    cardDescription: z.string(),
    cardDate: z.string(),
    featured: z.boolean().optional(),
    faq: z.array(z.object({ question: z.string(), answer: z.string() })).optional(),
  }),
});
```

From src/components/BlogCard.astro:

```typescript
interface Props {
  title: string;
  description: string;
  href: string;
  date: string;
  readTime?: string;
  featured?: boolean;
}
```

From src/layouts/BlogPostLayout.astro:

```typescript
interface Props {
  frontmatter: {
    title: string;
    headline: string;
    description: string;
    readTime?: string;
    metaLabel?: string;
    // ... other fields
  };
}
// readTime already displayed: line 113
const metaDisplay = [fm.metaLabel, fm.readTime].filter(Boolean).join(' · ');
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tags to blog schema and all 16 existing posts</name>
  <files>src/content.config.ts, src/content/blog/*.mdx (all 16 files)</files>
  <action>
1. In `src/content.config.ts`, add `tags` field to the blog schema:
   ```typescript
   tags: z.array(z.string()).default([]),
   ```
   Place it after `featured`. This allows tags to be optional with empty default.

2. Add appropriate `tags` arrays to all 16 existing blog post frontmatters. Use a consistent taxonomy. The tag set should be:
   - `penetration-testing` — for posts about pentest methodology, pricing, process
   - `compliance` — for BNR, ISO 27001, PCI DSS, SWIFT CSP content
   - `mobile-security` — for mobile banking, mobile money, USSD topics
   - `east-africa` — for region-specific threat landscape, market analysis
   - `banking-security` — for bank-specific security (API security, fraud, assessments)
   - `security-strategy` — for cost/value, choosing providers, vulnerability management

   Tag assignments (2-4 tags per post, based on actual content):
   - `api-security-banking.mdx`: [banking-security, penetration-testing]
   - `bank-fraud-east-africa-cybersecurity-lessons.mdx`: [banking-security, east-africa]
   - `bnr-cybersecurity-requirements.mdx`: [compliance, east-africa, banking-security]
   - `data-breach-cost-east-africa.mdx`: [east-africa, security-strategy]
   - `east-africa-cybersecurity.mdx`: [east-africa, security-strategy]
   - `iso-27001-rwanda.mdx`: [compliance, security-strategy]
   - `mobile-banking-security-assessment.mdx`: [mobile-security, banking-security, penetration-testing]
   - `mobile-money-security-testing.mdx`: [mobile-security, penetration-testing, east-africa]
   - `penetration-testing-cost-rwanda.mdx`: [penetration-testing, security-strategy, east-africa]
   - `penetration-testing-rwanda.mdx`: [penetration-testing, compliance, east-africa]
   - `penetration-testing-vs-vulnerability-assessment-banks.mdx`: [penetration-testing, banking-security, security-strategy]
   - `penetration-testing-vs-vulnerability-scanning.mdx`: [penetration-testing, security-strategy]
   - `social-engineering-east-africa-banks.mdx`: [penetration-testing, banking-security, east-africa]
   - `swift-csp-compliance-rwanda.mdx`: [compliance, banking-security, east-africa]
   - `ussd-security-testing.mdx`: [mobile-security, penetration-testing, east-africa]
   - `vapt-rwanda.mdx`: [penetration-testing, compliance, east-africa]

3. Verify the build passes with `npm run build` (Astro validates all frontmatter against schema).
   </action>
   <verify>
   <automated>cd /Users/aristofanischionis/Public/Projects/imizicyber.github.io && npm run build 2>&1 | tail -5</automated>
   </verify>
   <done>All 16 blog posts have tags arrays in frontmatter. The content.config.ts schema accepts tags. Build passes without errors.</done>
   </task>

<task type="auto">
  <name>Task 2: Add tag filtering to blog index and related posts to blog layout</name>
  <files>src/components/TagFilter.astro, src/components/RelatedPosts.astro, src/pages/blog/index.astro, src/layouts/BlogPostLayout.astro</files>
  <action>
1. **Create `src/components/TagFilter.astro`** — a client-side tag filter for the blog index:
   - Accept `tags: string[]` prop (the complete list of unique tags from all posts)
   - Render an "All" button plus one button per tag, styled consistently with the site (use var(--acc), var(--card), var(--white), var(--txt2))
   - Buttons should use `data-tag` attributes. "All" has `data-tag="all"`.
   - Include a `<script>` block that:
     - On click, adds `active` class to clicked button, removes from others
     - Shows/hides `.blog-card` elements based on their `data-tags` attribute (comma-separated tag list)
     - "All" shows everything
   - Style buttons as pill-shaped (border-radius: 20px, padding: 6px 16px, font-size: 0.78rem, font-family: var(--mono), letter-spacing: 0.04em, text-transform: uppercase)
   - Active button: background var(--acc), color var(--bg). Inactive: background var(--card), color var(--txt2), border: 1px solid var(--border).
   - Add `gap: 8px` flex wrap layout.

2. **Update `src/pages/blog/index.astro`**:
   - Import TagFilter component
   - Collect unique tags from all posts: `const allTags = [...new Set(posts.flatMap(p => p.data.tags || []))].sort()`
   - Render `<TagFilter tags={allTags} />` between the subtitle and the blog grid
   - Add `data-tags={post.data.tags?.join(',') || ''}` attribute to each BlogCard wrapper (wrap each BlogCard in a div with the data-tags attribute and class `blog-card-wrapper`)
   - Pass tags to BlogCard to display on cards: add a `tags` prop to BlogCard

3. **Update `src/components/BlogCard.astro`**:
   - Add optional `tags?: string[]` prop
   - Display tags below the date line if present, using the same mono font style as dates but smaller (font-size: 0.62rem, color: var(--txt2))
   - Format as comma-separated lowercase text

4. **Create `src/components/RelatedPosts.astro`**:
   - Accept props: `currentSlug: string`, `currentTags: string[]`
   - Import `getCollection` from `astro:content`
   - Fetch all blog posts, filter out the current post by slug
   - Score each post by number of shared tags with current post
   - Sort by score descending, take top 3 (minimum 1 shared tag)
   - If fewer than 2 related posts found, show nothing (return empty)
   - Render a section with heading "Related articles" using the same card style as BlogCard but simplified (title + read time + link)
   - Style: grid with 1-3 columns (auto-fill, minmax(220px, 1fr)), gap 16px
   - Use the same card background (var(--card)), border (var(--border)), and hover styles as existing blog cards

5. **Update `src/layouts/BlogPostLayout.astro`**:
   - Import RelatedPosts component
   - Extract the current post's tags from frontmatter (add `tags?: string[]` to the Props interface)
   - Render `<RelatedPosts currentSlug={slug} currentTags={fm.tags || []} />` between the article body slot and the CtaBox
   - The slug can be derived from canonicalUrl: `const slug = fm.canonicalUrl.replace('/blog/', '').replace(/\/$/, '')`

IMPORTANT: The tag filter must work client-side with JavaScript. Use `document.querySelectorAll` to show/hide cards. The blog is statically generated so this must be a progressive enhancement — all posts visible by default without JS.
</action>
<verify>
<automated>cd /Users/aristofanischionis/Public/Projects/imizicyber.github.io && npm run build 2>&1 | tail -5</automated>
</verify>
<done>Blog index shows clickable tag filter buttons that narrow displayed posts. Each blog post page shows 2-3 related posts below the article. Read time remains visible on all cards and posts. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Blog index page HTML contains tag filter buttons and data-tags attributes on cards
- Blog post pages contain RelatedPosts section markup
- Read time visible in both blog index cards and blog post headers (already implemented, verify not broken)
</verification>

<success_criteria>

- Tag filter buttons appear on /blog/ and clicking a tag shows only matching posts
- Each blog post shows related articles section with 2-3 posts sharing tags
- Read time visible on blog index cards (e.g., "MAR 2026 · 12 MIN READ") and at top of each post
- All 16 existing posts have tags in frontmatter
- Build passes cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/08-blog-system-and-new-content/08-01-SUMMARY.md`
</output>
