---
phase: 02-security-hardening-and-test-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - playwright.config.ts
  - e2e/fixtures/axe-test.ts
  - e2e/homepage.spec.ts
  - e2e/navigation.spec.ts
  - e2e/blog.spec.ts
  - e2e/contact-form.spec.ts
  - e2e/quiz.spec.ts
  - e2e/cookie-banner.spec.ts
  - e2e/accessibility.spec.ts
  - e2e/csp.spec.ts
  - .github/workflows/test-e2e.yml
  - package.json
autonomous: true
requirements:
  - QUAL-07
  - QUAL-12
must_haves:
  truths:
    - 'Playwright E2E tests cover homepage load, contact form, blog navigation, and quiz completion'
    - 'axe-core accessibility scans report zero critical WCAG 2.1 AA violations on all tested pages'
    - 'CSP E2E test verifies no unsafe-inline in the built site'
    - 'E2E tests run in CI via GitHub Actions'
    - 'E2E tests do NOT run in pre-commit hook'
  artifacts:
    - path: 'playwright.config.ts'
      provides: 'Playwright config with Astro webServer'
      contains: 'webServer'
    - path: 'e2e/fixtures/axe-test.ts'
      provides: 'Shared AxeBuilder fixture for accessibility testing'
      contains: 'AxeBuilder'
    - path: 'e2e/homepage.spec.ts'
      provides: 'Homepage E2E tests'
      contains: 'test.describe'
    - path: 'e2e/quiz.spec.ts'
      provides: 'Quiz completion E2E test'
      contains: 'score'
    - path: 'e2e/accessibility.spec.ts'
      provides: 'axe-core scans across all pages'
      contains: 'violations'
    - path: '.github/workflows/test-e2e.yml'
      provides: 'CI workflow for E2E tests'
      contains: 'playwright test'
  key_links:
    - from: 'e2e/*.spec.ts'
      to: 'playwright.config.ts'
      via: 'Playwright reads config for baseURL and webServer'
      pattern: 'baseURL.*localhost'
    - from: 'e2e/accessibility.spec.ts'
      to: 'e2e/fixtures/axe-test.ts'
      via: 'Imports shared axe fixture'
      pattern: 'import.*axe-test'
    - from: '.github/workflows/test-e2e.yml'
      to: 'playwright.config.ts'
      via: 'CI runs npx playwright test which reads config'
      pattern: 'npx playwright test'
---

<objective>
Install Playwright with axe-core and write comprehensive E2E tests covering all critical user journeys, plus a CI workflow.

Purpose: The site must be tested end-to-end to catch regressions before deployment. Accessibility testing ensures the site is usable by all visitors. E2E tests verify that the CSP hardening and innerHTML elimination from Plans 01-02 did not break functionality.

Output: ~10-15 Playwright E2E tests, axe-core accessibility scans, CSP verification test, GitHub Actions CI workflow.
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-hardening-and-test-coverage/02-CONTEXT.md
@.planning/phases/02-security-hardening-and-test-coverage/02-RESEARCH.md
@.planning/phases/02-security-hardening-and-test-coverage/02-01-SUMMARY.md
@.planning/phases/02-security-hardening-and-test-coverage/02-02-SUMMARY.md

<interfaces>
<!-- Site pages to test (from src/pages/) -->
Pages available:
- / (homepage)
- /blog/ (blog index)
- /blog/[slug]/ (individual blog posts)
- /services/penetration-testing/
- /services/security-assessments/
- /services/security-training/
- /services/custom-security-tools/
- /tools/security-score/ (quiz)
- /contact/
- /company-profile/ (standalone, no BaseLayout)
- /resources/
- /404 (error page)

Key DOM elements:

- Theme toggle: `[data-testid="theme-toggle"]` or button with theme toggle function
- Cookie banner: cookie consent component
- Contact form: form on /contact/ page
- Quiz: `.quiz-card`, `.option`, `#calc-btn`, `#result-section`, `#score-num`
- Navigation: nav links to all pages
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright, create config, axe fixture, and all E2E test files</name>
  <files>package.json, playwright.config.ts, e2e/fixtures/axe-test.ts, e2e/homepage.spec.ts, e2e/navigation.spec.ts, e2e/blog.spec.ts, e2e/contact-form.spec.ts, e2e/quiz.spec.ts, e2e/cookie-banner.spec.ts, e2e/accessibility.spec.ts, e2e/csp.spec.ts</files>
  <read_first>
    - package.json
    - src/pages/index.astro
    - src/pages/blog/index.astro
    - src/pages/contact.astro
    - src/pages/tools/security-score/index.astro
    - src/components/CookieBanner.astro
    - src/components/Nav.astro
    - src/layouts/BaseLayout.astro
  </read_first>
  <action>
**Step 1: Install dependencies.**

```bash
npm install -D @playwright/test @axe-core/playwright
npx playwright install chromium
```

Add npm scripts to package.json:

```json
"test:e2e": "npx playwright test",
"test:e2e:ui": "npx playwright test --ui"
```

**Step 2: Create `playwright.config.ts`.**

Use the configuration from RESEARCH.md Pattern 1:

- `testDir: './e2e'`
- `fullyParallel: true`
- `forbidOnly: !!process.env.CI`
- `retries: process.env.CI ? 2 : 0`
- `workers: process.env.CI ? 1 : undefined`
- `reporter: 'html'`
- `use.baseURL: 'http://localhost:4321'`
- `use.trace: 'on-first-retry'`
- Single project: chromium with Desktop Chrome device
- `webServer.command: 'npm run build && npm run preview'`
- `webServer.url: 'http://localhost:4321'`
- `webServer.reuseExistingServer: !process.env.CI`
- `webServer.timeout: 120_000`

**Step 3: Create `e2e/fixtures/axe-test.ts`.**

Use RESEARCH.md Pattern 2: extend Playwright test with `makeAxeBuilder` fixture that returns `new AxeBuilder({ page }).withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'])`.

**Step 4: Create E2E test files.**

Create the following test files. Each test should be practical and verify real user behavior:

**`e2e/homepage.spec.ts`** (~4 tests):

- Page loads with correct title containing "Imizi Cyber"
- Hero section is visible with h1
- "Book a Consultation" CTA is visible above the fold
- Theme toggle switches between dark and light mode (check `data-theme` attribute on `<html>`)

**`e2e/navigation.spec.ts`** (~3 tests):

- All main nav links are present and clickable
- Service pages load without errors (check each service page returns 200)
- 404 page displays for non-existent routes

**`e2e/blog.spec.ts`** (~3 tests):

- Blog index loads and shows blog post cards
- Clicking a blog post card navigates to the post page
- Blog post page has title, content, and back link

**`e2e/contact-form.spec.ts`** (~2 tests):

- Contact form is visible with name, email, message fields
- Form validation prevents empty submission (check for required attribute behavior)
- Do NOT actually submit to Formspree in tests

**`e2e/quiz.spec.ts`** (~2 tests):

- Quiz page loads with first question card visible
- Complete quiz flow: click an option on each of the 10 questions (use `.quiz-card.active .option:first-child`), wait for auto-advance (500ms + buffer), click `#calc-btn`, verify `#result-section` is visible and `#score-num` has a numeric value

**`e2e/cookie-banner.spec.ts`** (~2 tests):

- Cookie banner appears on first visit (no localStorage)
- Accepting cookies hides the banner and persists the choice

**`e2e/csp.spec.ts`** (~1 test):

- Load homepage, extract CSP meta tag content, assert it does NOT contain `unsafe-inline`

**`e2e/accessibility.spec.ts`** (~3 tests):

- Homepage has zero axe-core violations (using axe fixture)
- Blog index has zero axe-core violations
- Contact page has zero axe-core violations
- If any page has known violations that cannot be fixed in this phase (e.g., color contrast from Phase 3), document them and use `exclude` to skip those specific elements, with a TODO comment for the relevant phase.

Total: ~20 tests across 8 files. Each test should have descriptive names. Use `test.describe` to group tests per file.

**Step 5: Run the tests.**

```bash
npx playwright test
```

Fix any failing tests. Tests that fail due to site content issues (not test bugs) should be noted in the summary.

Add `playwright-report/`, `test-results/`, and `e2e/results/` to `.gitignore` if not already present.
</action>
<verify>
<automated>npx playwright test --reporter=list 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `npx playwright test` exits 0 (all tests pass) - `ls e2e/*.spec.ts | wc -l` returns 8 (eight test files) - `ls e2e/fixtures/axe-test.ts` exists - `ls playwright.config.ts` exists - `grep "webServer" playwright.config.ts` returns a match - `grep "@axe-core/playwright" package.json` returns a match - `grep "@playwright/test" package.json` returns a match - `grep "unsafe-inline" e2e/csp.spec.ts` appears in a test assertion (verifying absence) - `grep "AxeBuilder" e2e/fixtures/axe-test.ts` returns a match
</acceptance_criteria>
<done>Playwright installed. 8 E2E test files with ~20 tests covering homepage, navigation, blog, contact form, quiz, cookie banner, CSP, and accessibility. All tests pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow for E2E tests</name>
  <files>.github/workflows/test-e2e.yml</files>
  <read_first>
    - .github/workflows/deploy.yml
    - playwright.config.ts
    - package.json
  </read_first>
  <action>
Create `.github/workflows/test-e2e.yml` using the pattern from RESEARCH.md:

```yaml
name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run Playwright tests
        run: npx playwright test

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
```

Key decisions:

- Runs on push to main AND pull requests (catches regressions before merge)
- Uses `.nvmrc` for Node version (consistent with deploy.yml)
- `--with-deps` installs system dependencies needed by Chromium on Ubuntu
- Report artifact uploaded even on failure for debugging
- Separate from deploy workflow to keep concerns isolated

Ensure the workflow file is valid YAML. Do NOT add gitleaks to CI — it is handled by the pre-commit hook locally. A future CI integration can use `gitleaks/gitleaks-action@v2` if desired.
</action>
<verify>
<automated>cat .github/workflows/test-e2e.yml | head -5 && echo "PASS: workflow file exists"</automated>
</verify>
<acceptance_criteria> - `.github/workflows/test-e2e.yml` exists - `grep "playwright test" .github/workflows/test-e2e.yml` returns a match - `grep "upload-artifact" .github/workflows/test-e2e.yml` returns a match - `grep "chromium" .github/workflows/test-e2e.yml` returns a match - `grep "node-version-file" .github/workflows/test-e2e.yml` returns a match - Workflow triggers on both push to main and pull_request
</acceptance_criteria>
<done>GitHub Actions CI workflow for E2E tests created. Runs Playwright on push to main and PRs. Uploads test report as artifact.</done>
</task>

</tasks>

<verification>
- `npx playwright test` exits 0
- `ls e2e/*.spec.ts | wc -l` returns 8
- `.github/workflows/test-e2e.yml` exists and is valid
- `npm run test -- --run` still passes (unit tests unaffected)
- `npm run typecheck` exits 0
</verification>

<success_criteria>

1. Playwright E2E tests cover all critical user journeys: homepage, navigation, blog, contact form, quiz, cookie banner
2. axe-core accessibility scans integrated into Playwright tests with zero critical violations
3. CSP verification test confirms no unsafe-inline in built site
4. CI workflow runs E2E tests on push and PRs
5. E2E tests are NOT in the pre-commit hook (kept fast)
   </success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening-and-test-coverage/02-03-SUMMARY.md`
</output>
