---
phase: 09-lead-generation-funnel
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/pages/tools/security-score/index.astro
  - src/pages/resources/index.astro
autonomous: false
requirements:
  - LEAD-04
  - LEAD-07
  - LEAD-08
user_setup:
  - service: formspree
    why: 'Auto-responder email for resource downloads and quiz completions'
    env_vars: []
    dashboard_config:
      - task: 'Enable auto-responder on form'
        location: 'Formspree Dashboard -> Form (mjgerrlv) -> Settings -> Auto-Response'
        details: "Set up an auto-reply email that thanks the user and includes a link to book a consultation. Subject: 'Your security resource from Imizi Cyber'. Body should thank them, reference the resource they downloaded, and include a CTA to book a consultation."
must_haves:
  truths:
    - 'After submitting the quiz gate form, the visitor receives a follow-up email from Formspree'
    - 'After submitting a resource download form, the visitor receives a follow-up email from Formspree'
    - 'The quiz page has no innerHTML usage (LEAD-04 already resolved but verified)'
    - 'All 8 LEAD requirements are satisfied end-to-end'
  artifacts:
    - path: 'src/pages/tools/security-score/index.astro'
      provides: 'Quiz page with form action pointing to Formspree'
    - path: 'src/pages/resources/index.astro'
      provides: 'Resource page with form action pointing to Formspree'
  key_links:
    - from: 'Quiz gate-form'
      to: 'Formspree'
      via: 'fetch POST to formspreeUrl'
      pattern: 'formspreeUrl'
    - from: 'Resource download form'
      to: 'Formspree'
      via: 'fetch POST to formspreeUrl'
      pattern: 'formspreeUrl'
---

<objective>
Configure Formspree auto-responder for email follow-up and verify all lead generation requirements are met.

Purpose: Close the loop on lead capture — every person who downloads a resource or completes the quiz receives a follow-up email, and all LEAD requirements are verified working.
Output: Formspree auto-responder configured, full phase verification completed.
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/tools/security-score/index.astro
@src/pages/resources/index.astro
@src/scripts/resources.ts
@src/scripts/quiz/ui.ts
@src/data/site.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Formspree form submissions include email field and add _replyto for auto-responder</name>
  <files>src/pages/tools/security-score/index.astro, src/pages/resources/index.astro</files>
  <action>
Formspree's built-in auto-responder feature sends a reply to the email address submitted in the form. For this to work, the form must include a field named `email` (which both forms already do) and optionally a `_replyto` hidden field.

1. In `src/pages/tools/security-score/index.astro`:
   - The quiz gate form already has `<input type="email" id="g-email" name="email">` which Formspree will use as the reply-to address. No changes needed to the form markup.
   - Verify the form submits to `ANALYTICS.formspreeUrl` (already confirmed in ui.ts).

2. In `src/pages/resources/index.astro`:
   - Check that each resource form has an `email` field with `name="email"`. If the field uses a different name attribute, rename it to `email` so Formspree auto-responder can find the recipient address.
   - If any resource form is missing an email field, add one.

3. Both forms already POST to Formspree via fetch in their respective scripts (ui.ts and resources.ts). The auto-responder is configured in the Formspree dashboard, not in code. The code changes here are minimal — just ensuring the email field naming is correct.

4. Run `npm run build` to verify no regressions.

Note on LEAD-04: innerHTML was already eliminated from the quiz in Phase 2 (decision logged in STATE.md). Verify with `grep -r "innerHTML" src/scripts/quiz/` — should return no matches.
</action>
<verify>
<automated>cd /Users/aristofanischionis/Public/Projects/imizicyber.github.io && grep -r "innerHTML" src/scripts/quiz/ && echo "FAIL: innerHTML found" || echo "PASS: no innerHTML in quiz" && npm run build 2>&1 | tail -3</automated>
</verify>
<done>Both quiz and resource forms submit email to Formspree correctly, no innerHTML in quiz code, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full lead generation funnel and configure Formspree auto-responder</name>
  <files>n/a</files>
  <action>
Human verification of all lead generation infrastructure built across plans 01, 02, and 03. User must also configure Formspree auto-responder in the dashboard (cannot be done via CLI/API).

Steps for the user:

1. Configure Formspree auto-responder at https://formspree.io/forms/mjgerrlv/settings
2. Test all 6 success criteria end-to-end
   </action>
   <verify>User confirms all checks pass</verify>
   <done>All 8 LEAD requirements verified working, Formspree auto-responder sends follow-up emails</done>
   </task>

</tasks>

<verification>
- All 8 LEAD requirements verified:
  - LEAD-01: WhatsApp float uses service-specific messages (grep encodeURIComponent in WhatsAppFloat.astro)
  - LEAD-02: WhatsApp clicks tracked (trackEvent in WhatsAppFloat.astro and service pages)
  - LEAD-03: Resource downloads tracked with document name (trackEvent in resources.ts)
  - LEAD-04: No innerHTML in quiz (grep returns empty)
  - LEAD-05: Quiz completion fires GA4 event (trackEvent in ui.ts)
  - LEAD-06: jsPDF bundled locally (import jsPDF from 'jspdf' in pdf.ts)
  - LEAD-07: Formspree auto-responder configured for email follow-up
  - LEAD-08: Quiz linked from homepage and navigation
- `npm run build` succeeds
- `npm run typecheck` passes
</verification>

<success_criteria>

- Formspree auto-responder sends follow-up email after form submission
- All 6 success criteria from the phase goal are met
- No technical debt remains in the lead generation funnel
  </success_criteria>

<output>
After completion, create `.planning/phases/09-lead-generation-funnel/09-03-SUMMARY.md`
</output>
