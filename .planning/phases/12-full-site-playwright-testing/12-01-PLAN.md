---
phase: 12-full-site-playwright-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/page-routes.spec.ts
  - e2e/interactions.spec.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - 'Every page route returns HTTP 200 and has a visible h1 element'
    - 'No page produces console errors during load'
    - 'Theme toggle switches between dark and light mode and persists across reload'
    - 'WhatsApp float link is visible and has correct wa.me href with pre-filled text'
    - 'Nav hamburger opens and closes the mobile menu'
    - 'Blog tag filter buttons show and hide posts by tag'
  artifacts:
    - path: 'e2e/page-routes.spec.ts'
      provides: 'Smoke tests for all 20+ page routes'
      contains: 'page.goto'
    - path: 'e2e/interactions.spec.ts'
      provides: 'Interactive element tests for theme toggle, WhatsApp, hamburger, tag filter'
      contains: 'theme-toggle-btn'
  key_links:
    - from: 'e2e/page-routes.spec.ts'
      to: 'All src/pages/ routes'
      via: 'page.goto with route paths'
      pattern: "page\\.goto\\("
    - from: 'e2e/interactions.spec.ts'
      to: 'src/components/ThemeToggle.astro'
      via: '#theme-toggle-btn click'
      pattern: 'theme-toggle-btn'
    - from: 'e2e/interactions.spec.ts'
      to: 'src/components/Nav.astro'
      via: '.menu-btn click and .nav-links class check'
      pattern: 'menu-btn'
---

<objective>
Create two new spec files covering all page route smoke tests and untested interactive elements.

Purpose: TEST-01 requires every page to have E2E coverage (200 status, h1, no console errors). TEST-02 requires testing all interactive elements not yet covered by existing specs (theme toggle, WhatsApp links, nav hamburger, blog tag filter).
Output: `e2e/page-routes.spec.ts` and `e2e/interactions.spec.ts`
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-full-site-playwright-testing/12-CONTEXT.md
@.planning/phases/12-full-site-playwright-testing/12-RESEARCH.md

@playwright.config.ts
@e2e/navigation.spec.ts
@e2e/accessibility.spec.ts

<interfaces>
<!-- Key selectors and patterns the executor needs -->

From playwright.config.ts:

- baseURL: http://localhost:4321
- webServer builds + previews before tests
- fullyParallel: true, retries: 2 in CI

From e2e/navigation.spec.ts (existing pattern):

```typescript
// Route testing pattern already established:
const response = await page.goto(path);
expect(response?.status()).toBe(200);
```

From e2e/accessibility.spec.ts (cookie banner dismissal pattern):

```typescript
// Cookie banner must be dismissed before interaction tests:
const cookieBanner = page.locator('#cookie-banner');
if (await cookieBanner.isVisible()) {
  await page.locator('#cookie-reject').click();
  await expect(cookieBanner).not.toBeVisible();
}
```

Key DOM selectors:

- Theme toggle button: `#theme-toggle-btn`
- Theme attribute: `html[data-theme]` (values: "dark", "light")
- Nav hamburger: `.menu-btn`
- Nav links container: `.nav-links` (gets class `open` when expanded)
- WhatsApp float: `a[href*="wa.me"]`
- Blog tag buttons: `[data-tag]`
- Blog cards: `.blog-card-wrapper` with `data-tags` attribute
- Cookie banner: `#cookie-banner`
- Cookie reject: `#cookie-reject`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create page-routes.spec.ts with all route smoke tests</name>
  <files>e2e/page-routes.spec.ts</files>
  <read_first>
    - e2e/navigation.spec.ts (existing route testing pattern to follow)
    - playwright.config.ts (baseURL, webServer config)
  </read_first>
  <action>
Create `e2e/page-routes.spec.ts` with smoke tests for every page route. Use describe blocks grouped by page type per CONTEXT.md locked decision.

Route groups and paths:

```
Core: /, /about/, /blog/, /resources/
Services: /services/penetration-testing/, /services/security-assessments/, /services/managed-security/, /services/custom-tooling/, /services/security-training/
Content: /case-studies/, /case-studies/east-africa-bank-pentest/, /case-studies/mobile-money-security-assessment/, /company-profile/, /privacy-policy/, /responsible-disclosure/
Tools: /tools/security-score/
Blog Posts: /blog/penetration-testing-rwanda/ (one representative post)
```

For each route, test in a single test case:

1. Listen for console errors via `page.on('console', msg => { if (msg.type() === 'error') consoleErrors.push(msg.text()); })`
2. Navigate with `const response = await page.goto(path)`
3. Assert `expect(response?.status()).toBe(200)`
4. Assert `await expect(page.locator('h1')).toBeVisible()`
5. Assert `expect(consoleErrors).toEqual([])`

Use `test.describe` with group names like `'Page Routes: Core'`, `'Page Routes: Services'`, etc.

Use a loop pattern:

```typescript
for (const { group, paths } of allRoutes) {
  test.describe(`Page Routes: ${group}`, () => {
    for (const path of paths) {
      test(`${path} returns 200 with h1 and no console errors`, async ({ page }) => {
```

Import from `@playwright/test` (not the axe fixture).
</action>
<verify>
<automated>npx playwright test page-routes.spec.ts --reporter=list 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - e2e/page-routes.spec.ts exists and contains `import { test, expect } from '@playwright/test'` - File contains `test.describe('Page Routes: Core'` - File contains `test.describe('Page Routes: Services'` - File contains `test.describe('Page Routes: Content'` - File contains `test.describe('Page Routes: Tools'` - File contains `test.describe('Page Routes: Blog Posts'` - File contains paths: `/`, `/about/`, `/blog/`, `/resources/`, `/services/penetration-testing/`, `/services/security-assessments/`, `/services/managed-security/`, `/services/custom-tooling/`, `/services/security-training/`, `/case-studies/`, `/case-studies/east-africa-bank-pentest/`, `/case-studies/mobile-money-security-assessment/`, `/company-profile/`, `/privacy-policy/`, `/responsible-disclosure/`, `/tools/security-score/`, `/blog/penetration-testing-rwanda/` - File contains `consoleErrors` array pattern with `page.on('console'` - File contains `expect(response?.status()).toBe(200)` - File contains `page.locator('h1')` - `npx playwright test page-routes.spec.ts` exits 0
</acceptance_criteria>
<done>All 17 page routes tested for 200 status, h1 presence, and zero console errors. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create interactions.spec.ts with untested interactive element tests</name>
  <files>e2e/interactions.spec.ts</files>
  <read_first>
    - e2e/accessibility.spec.ts (cookie banner dismissal pattern, hamburger Escape test pattern)
    - e2e/navigation.spec.ts (existing nav testing patterns)
    - playwright.config.ts (config reference)
  </read_first>
  <action>
Create `e2e/interactions.spec.ts` with tests for interactive elements not covered by existing spec files. Import from `@playwright/test`.

Include a `test.beforeEach` that navigates to `/` and dismisses the cookie banner if visible:

```typescript
test.beforeEach(async ({ page }) => {
  await page.goto('/');
  const cookieBanner = page.locator('#cookie-banner');
  if (await cookieBanner.isVisible()) {
    await page.locator('#cookie-reject').click();
    await expect(cookieBanner).not.toBeVisible();
  }
});
```

Test 1 — `'theme toggle switches between dark and light mode'`:

- Locate `#theme-toggle-btn`
- Get initial theme: `await page.locator('html').getAttribute('data-theme')`
- Click toggle, verify `data-theme` changed (if was `dark`, now `light` or vice versa)
- Click again, verify it toggled back
- Click once more to change, reload page, verify the last set value persists (localStorage persistence)
- Use `await expect(page.locator('html')).toHaveAttribute('data-theme', expectedValue)`

Test 2 — `'theme toggle persists across page navigation'`:

- Get current theme, click toggle to change it
- Navigate to `/about/` (different page)
- Verify `data-theme` matches the toggled value

Test 3 — `'WhatsApp float link has correct href'`:

- Locate `a[href*="wa.me"]`
- Assert first match is visible: `await expect(whatsappLink.first()).toBeVisible()`
- Get href attribute, assert it contains `wa.me`
- Assert href contains `text=` (pre-filled message)

Test 4 — `'WhatsApp link on service page has service-specific message'`:

- Navigate to `/services/penetration-testing/`
- Dismiss cookie banner if visible
- Locate `a[href*="wa.me"]`
- Get href, assert it contains `text=` with non-empty encoded text
- Assert the text content is different from homepage (service-specific)

Test 5 — `'nav hamburger opens and closes menu on mobile'`:

- Set viewport to 375x667: `await page.setViewportSize({ width: 375, height: 667 })`
- Locate `.menu-btn` and `.nav-links`
- Click `.menu-btn`, assert `.nav-links` has class matching `/open/`
- Click `.menu-btn` again, assert `.nav-links` does NOT have class matching `/open/`

Test 6 — `'blog tag filter shows/hides posts'`:

- Navigate to `/blog/`
- Dismiss cookie banner if visible
- Locate tag buttons with `[data-tag]`
- If no tags found (count === 0), skip with `test.skip()`
- Get total count of `.blog-card-wrapper` elements
- Click first tag button
- Count visible `.blog-card-wrapper` elements (use `page.locator('.blog-card-wrapper:not([style*="display: none"])')` or check visibility)
- Assert visible count > 0 and visible count <= total count
- Click the "All" tag or same tag again to deactivate
- Assert all cards visible again

Wrap all tests in `test.describe('Interactive Elements', () => { ... })`.
</action>
<verify>
<automated>npx playwright test interactions.spec.ts --reporter=list 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - e2e/interactions.spec.ts exists and contains `import { test, expect } from '@playwright/test'` - File contains `test.describe('Interactive Elements'` - File contains `#theme-toggle-btn` selector - File contains `data-theme` attribute check - File contains `a[href*="wa.me"]` selector - File contains `page.setViewportSize({ width: 375, height: 667 })` for hamburger test - File contains `.menu-btn` and `.nav-links` selectors - File contains `[data-tag]` selector for tag filter test - File contains `#cookie-banner` dismissal in beforeEach - `npx playwright test interactions.spec.ts` exits 0
</acceptance_criteria>
<done>Theme toggle (switch + persistence), WhatsApp links (homepage + service page), nav hamburger (open/close), and blog tag filter all tested and passing.</done>
</task>

</tasks>

<verification>
```bash
# Verify both new spec files pass
npx playwright test page-routes.spec.ts interactions.spec.ts --reporter=list

# Verify full suite still passes (no regressions)

npx playwright test --reporter=list 2>&1 | tail -5

```
</verification>

<success_criteria>
- `e2e/page-routes.spec.ts` covers all 17 page routes with 200/h1/console-error checks
- `e2e/interactions.spec.ts` covers theme toggle, WhatsApp links, hamburger menu, and tag filter
- Both new specs pass individually
- Full Playwright suite passes with no regressions
- Suite runtime remains under 2 minutes
</success_criteria>

<output>
After completion, create `.planning/phases/12-full-site-playwright-testing/12-01-SUMMARY.md`
</output>
```
