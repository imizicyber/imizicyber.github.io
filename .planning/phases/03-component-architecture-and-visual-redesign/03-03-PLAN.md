---
phase: 03-component-architecture-and-visual-redesign
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/components/sections/ProcessSection.astro
  - src/components/sections/BlogSection.astro
  - src/components/sections/FaqSection.astro
  - src/components/sections/CtaSection.astro
  - src/components/sections/ContactSection.astro
  - src/pages/index.astro
  - src/styles/global.css
autonomous: true
requirements: [UIUX-01]

must_haves:
  truths:
    - 'All 10 homepage sections are standalone Astro components'
    - 'index.astro is under 100 lines, composing only component imports and BaseLayout wrapper'
    - 'BlogSection dynamically fetches from content collection instead of hardcoded array'
    - 'index.astro contains no inline section markup'
    - 'global.css has no orphaned homepage-section CSS'
    - 'Homepage renders identically to current site with all sections visible'
  artifacts:
    - path: 'src/components/sections/ProcessSection.astro'
      provides: '4-step engagement process'
      contains: 'process'
    - path: 'src/components/sections/BlogSection.astro'
      provides: 'Latest blog posts grid with dynamic content collection fetch'
      contains: 'getCollection'
    - path: 'src/components/sections/FaqSection.astro'
      provides: 'FAQ accordion'
      contains: 'faq-list'
    - path: 'src/components/sections/CtaSection.astro'
      provides: 'Compliance urgency CTA'
      contains: 'cta-section'
    - path: 'src/components/sections/ContactSection.astro'
      provides: 'Contact form and info'
      contains: 'ContactForm'
    - path: 'src/pages/index.astro'
      provides: 'Fully refactored homepage composing 10 section components'
      min_lines: 40
  key_links:
    - from: 'src/pages/index.astro'
      to: 'src/components/sections/BlogSection.astro'
      via: 'import BlogSection'
      pattern: 'BlogSection'
    - from: 'src/components/sections/BlogSection.astro'
      to: 'astro:content'
      via: "getCollection('blog')"
      pattern: 'getCollection'
    - from: 'src/components/sections/ContactSection.astro'
      to: 'src/components/ContactForm.astro'
      via: 'import ContactForm'
      pattern: 'ContactForm'
---

<objective>
Complete the homepage component extraction by extracting the remaining 5 sections (Process, Blog, FAQ, CTA, Contact), switch BlogSection to dynamic content collection, and clean up index.astro and global.css.

Purpose: After this plan, index.astro is fully refactored from 1284 lines to ~60-80 lines of pure component composition. No inline section markup remains. BlogSection uses Astro content collections for automatic freshness. global.css has no orphaned homepage-specific CSS.

Output: 5 new section component files, fully refactored index.astro, cleaned global.css.
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-CONTEXT.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-RESEARCH.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-UI-SPEC.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-01-SUMMARY.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: established component pattern -->
From src/components/sections/HeroSection.astro (pattern reference):
```astro
---
import { CONTACT } from '@/data/site';
---
<section class="hero">
  <div class="w"> ... </div>
</section>
<style> /* section-specific styles */ </style>
```

<!-- Astro content collection API -->

```typescript
import { getCollection } from 'astro:content';
const posts = (await getCollection('blog'))
  .sort(
    (a, b) => new Date(b.data.datePublished).getTime() - new Date(a.data.datePublished).getTime(),
  )
  .slice(0, 3);
```

<!-- Blog post data shape from content.config.ts -->

Each post has: post.data.title, post.data.cardDescription, post.data.cardDate, post.data.readTime, post.data.featured, post.id (slug)

<!-- ContactForm component (existing, used as-is) -->

From src/components/ContactForm.astro:

- Self-contained component, no props required
- Renders form with Formspree integration

<!-- Existing data exports -->

From src/data/site.ts:

```typescript
export const CONTACT = { whatsappUrl, phone, address, email, ... } as const;
export const SITE = { legalName, ... } as const;
```

<!-- E2E selectors to preserve -->

- `h1` -- homepage.spec.ts
- link[name=/consultation|get in touch/i] -- homepage.spec.ts
- `#theme-toggle-btn` -- homepage.spec.ts
- `#contact` section ID -- used by anchor links from nav and hero buttons
- `#blog` section ID -- used by anchor links
- `#services` section ID -- used by anchor links
- `#faq` section ID -- used by anchor links
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ProcessSection, BlogSection (dynamic), FaqSection, CtaSection, ContactSection</name>
  <files>src/components/sections/ProcessSection.astro, src/components/sections/BlogSection.astro, src/components/sections/FaqSection.astro, src/components/sections/CtaSection.astro, src/components/sections/ContactSection.astro</files>
  <read_first>
    - src/pages/index.astro (current state after Plan 02 -- remaining inline sections: Process at ~540-573, Blog at ~629-650, FAQ at ~652-668, CTA at ~670-700, Contact at ~702-747; remaining CSS in style block; and the faqItems + blogPosts arrays in frontmatter)
    - src/components/sections/HeroSection.astro (established component pattern from Plan 02)
    - src/components/ContactForm.astro (used by ContactSection, import path needed)
    - src/data/site.ts (CONTACT, SITE exports needed by CtaSection and ContactSection)
    - src/content.config.ts (blog collection schema -- understand data shape for BlogSection)
    - src/pages/blog/index.astro (reference for how getCollection('blog') is used in existing codebase)
    - .planning/phases/03-component-architecture-and-visual-redesign/03-UI-SPEC.md (BlogSection count prop, section order)
  </read_first>
  <action>
Create 5 new component files following the established pattern from Plan 02:

**ProcessSection.astro:** Extract the `<!-- PROCESS -->` section from index.astro.

- Contains `<section id="process" class="sec sec-b">` with 4 `.step` elements
- `<style>` block with process CSS from index.astro (`.process` grid, `.step`, `.step-num`, `.step-line`, `.step h4`, `.step p`)
- Include responsive: process grid 2-col on tablet (max-width: 1024px), 1-col on mobile (max-width: 768px)
- Include `.step-line { display: none !important; }` for tablet breakpoint

**BlogSection.astro:** Replace the hardcoded blog posts with dynamic content collection fetch.

- Accept a `count` prop (default: 3) via Astro Props interface:
  ```typescript
  interface Props {
    count?: number;
  }
  const { count = 3 } = Astro.props;
  ```
- Import `getCollection` from `astro:content`
- Fetch and sort blog posts by datePublished descending, take first `count`:
  ```typescript
  const allPosts = (await getCollection('blog'))
    .sort(
      (a, b) => new Date(b.data.datePublished).getTime() - new Date(a.data.datePublished).getTime(),
    )
    .slice(0, count);
  ```
- Render the blog grid using data from content collection:
  - `post.data.cardDate` for date display (fallback: format `post.data.datePublished`)
  - `post.data.readTime` for read time (fallback: empty string)
  - `post.data.title` for heading
  - `post.data.cardDescription` for description (fallback: `post.data.description`)
  - `post.data.featured` for the `.featured` class
  - `/blog/${post.id}/` for the href
- Section contains `id="blog"` (preserve anchor link target)
- Add "View All Posts" link after the grid: `<a href="/blog/" class="blog-view-all">View All Posts &#8594;</a>`
- `<style>` block with blog grid CSS: `.blog-grid` (already in global.css as shared), `.blog-card.featured` (grid-column span), responsive overrides from index.astro
- Add style for `.blog-view-all`:
  ```css
  .blog-view-all {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-top: 24px;
    font-size: 0.84rem;
    font-weight: 700;
    color: var(--acc);
    text-decoration: none;
    transition: gap 0.2s;
  }
  .blog-view-all:hover {
    gap: 10px;
  }
  ```
- Handle empty state: if `allPosts.length === 0`, show: `<p class="blog-empty">We are preparing insights on cybersecurity in East Africa. Check back soon.</p>`

**FaqSection.astro:** Extract the `<!-- FAQ -->` section from index.astro.

- Move the `faqItems` array INTO this component's frontmatter (it's only used here)
- Contains `<section id="faq" class="sec sec-b">` with details/summary FAQ accordion
- FAQ styles (`.faq-list`, `.faq`, `.faq summary`, `.faq-body`) are already in global.css and shared with other pages -- do NOT move them. Only add any faq-section-specific overrides if needed.
- No additional `<style>` block needed unless there are section-specific overrides (the current index.astro style block has no faq-specific CSS beyond what's in global.css)

**CtaSection.astro:** Extract the `<!-- CTA -->` section from index.astro.

- No props, no data imports needed (static content with hardcoded WhatsApp URL)
- Actually, import CONTACT from `@/data/site` for the WhatsApp URL instead of hardcoding it: replace `https://wa.me/250793146617?text=Hi%2C%20I%27d%20like%20to%20discuss%20BNR%20compliance%20for%20our%20institution` with a proper import. Or keep it hardcoded since this is a specific compliance-focused message different from the generic CONTACT.whatsappUrl. Decision: keep the hardcoded compliance-specific WhatsApp URL as-is since it has a custom message.
- Contains `<section class="cta-section">` with compliance urgency messaging
- `<style>` block with CTA section CSS from index.astro lines 1128-1148 (`.cta-section`, `.cta-sub`)

**ContactSection.astro:** Extract the `<!-- CONTACT -->` section from index.astro.

- Import `ContactForm` from `@/components/ContactForm.astro`
- Import `CONTACT` and `SITE` from `@/data/site`
- Contains `<section id="contact" class="sec">` with `.contact-grid`, contact info, and `<ContactForm />`
- `<style>` block with contact CSS from index.astro lines 1150-1196 (`.contact-grid`, `.contact-info`, `.contact-label`, `.contact-val`, `.contact-wa`)
- Include light theme overrides from lines 1212-1214
- Include responsive from line 1258-1260 (`.contact-grid` 1-col on mobile)

CRITICAL for all components:

- Preserve all `id` attributes (services, process, about, blog, faq, contact) -- these are anchor link targets from the nav and hero buttons
- Use existing global CSS classes (`.w`, `.sec`, `.sec-b`, `.sec-tag`, `.sec-title`, `.sec-sub`, `.reveal`, `.btn`, `.btns`, `.btn-fill`, `.btn-wa`) -- do NOT duplicate
- Each component is self-contained with its section-specific CSS in a `<style>` block
  </action>
  <verify>
  <automated>ls src/components/sections/ProcessSection.astro src/components/sections/BlogSection.astro src/components/sections/FaqSection.astro src/components/sections/CtaSection.astro src/components/sections/ContactSection.astro && grep -l "getCollection" src/components/sections/BlogSection.astro && grep -l "ContactForm" src/components/sections/ContactSection.astro</automated>
  </verify>
  <acceptance_criteria> - src/components/sections/ProcessSection.astro exists and contains `id="process"` - src/components/sections/ProcessSection.astro contains `<style>` block with `.process` grid - src/components/sections/BlogSection.astro exists and contains `getCollection('blog')` - src/components/sections/BlogSection.astro accepts `count` prop with default 3 - src/components/sections/BlogSection.astro contains `id="blog"` - src/components/sections/BlogSection.astro contains `View All Posts` link to `/blog/` - src/components/sections/BlogSection.astro handles empty state with fallback message - src/components/sections/FaqSection.astro exists and contains `faqItems` array in frontmatter - src/components/sections/FaqSection.astro contains `id="faq"` - src/components/sections/CtaSection.astro exists and contains `class="cta-section"` - src/components/sections/ContactSection.astro exists and contains `import ContactForm` - src/components/sections/ContactSection.astro contains `id="contact"` - All 5 files use existing global classes (.sec, .w, .reveal) -- not duplicated in style blocks
  </acceptance_criteria>
  <done>All 5 remaining section components created. BlogSection uses dynamic content collection. FaqSection contains its own data. ContactSection wraps existing ContactForm. All anchor link IDs preserved.</done>
  </task>

<task type="auto">
  <name>Task 2: Complete index.astro refactor and clean orphaned CSS from global.css</name>
  <files>src/pages/index.astro, src/styles/global.css</files>
  <read_first>
    - src/pages/index.astro (current state after Plan 02 -- still has inline sections for Process through Contact, plus blogPosts and faqItems arrays in frontmatter, plus remaining CSS)
    - src/components/sections/ProcessSection.astro (from Task 1, verify it exists)
    - src/components/sections/BlogSection.astro (from Task 1, verify it uses count prop)
    - src/components/sections/FaqSection.astro (from Task 1, verify it has faqItems internally)
    - src/components/sections/CtaSection.astro (from Task 1)
    - src/components/sections/ContactSection.astro (from Task 1)
    - src/styles/global.css (identify CSS that was ONLY used by homepage inline sections and is now orphaned)
    - e2e/homepage.spec.ts (selectors to preserve)
  </read_first>
  <action>
**Step 1: Finalize index.astro**

Rewrite index.astro to be a pure composition file. The final file should be approximately 60-80 lines containing:

1. Frontmatter with:
   - Import of BaseLayout
   - Import of all 10 section components from `../components/sections/`
   - Import of `SITE` from `../data/site`
   - The `schemas` array (Organization, FAQ, Website JSON-LD -- this stays in index.astro because it's page-specific structured data)
   - NOTE: Remove the `blogPosts` array (moved to BlogSection which uses content collection)
   - NOTE: Remove the `faqItems` array (moved into FaqSection)
   - NOTE: Remove the `ContactForm` import (moved into ContactSection)

2. Template with BaseLayout wrapper containing `<main id="main">` with 10 component tags in this order (per UI-SPEC.md):

   ```astro
   <HeroSection />
   <TrustBar />
   <CredentialsSection />
   <WhyUsSection />
   <ServicesSection />
   <ProcessSection />
   <BlogSection count={3} />
   <FaqSection />
   <CtaSection />
   <ContactSection />
   ```

3. Script block (keep existing):

   ```astro
   <script>
     import { initScrollReveal } from '@/scripts/scroll-reveal';
     import { initEmailObfuscation } from '@/scripts/email-obfuscation';
     initScrollReveal();
     initEmailObfuscation();
   </script>
   ```

4. REMOVE the entire `<style>` block from index.astro -- all CSS has been moved to component-scoped styles.

**Step 2: Clean orphaned CSS from global.css**

After index.astro is refactored, check global.css for CSS selectors that were ONLY used by the now-extracted homepage sections. The following homepage-specific styles from global.css should be evaluated:

- `.hero` padding (line 308-310) -- this might be shared with service pages. Check if service pages have their own `.hero` styles. If service pages use `.hero` class from global.css, KEEP it. If they override it in their own `<style>` block, it can be removed from global.css later (not in this plan -- service pages are not being refactored yet).
- KEEP in global.css (shared across pages): `.sec`, `.sec-b`, `.sec-tag`, `.sec-title`, `.sec-sub`, `.hero-badge`, `.card`, `.blog-card`, `.blog-date`, `.blog-card h2`, `.blog-card p`, `.blog-link`, `.faq-list`, `.faq`, `.faq summary`, `.faq-body`, `.form-box`, `.field`, `.submit-btn`, `.btn`, `.btns`, `.btn-fill`, `.btn-wa`, `.btn-ghost`, `.reveal`, `.w`, `.w-wide`, nav styles, footer styles, cookie banner, WhatsApp float, skip link, focus styles, keyframes, responsive nav/footer overrides.

DO NOT remove any CSS from global.css that is used by other pages (blog, services, resources, etc.). When in doubt, KEEP the CSS. Orphaned CSS cleanup is a minor optimization -- breaking other pages is a major regression.

The only safe removals from global.css at this stage:

- The `.hero { padding: 132px 0 48px; }` duplicate at line 308-310 IF and ONLY IF service pages do not reference this class from global.css. Check service page files first. If unsure, leave it.

Actually, to be safe: do NOT remove anything from global.css in this task. The risk of breaking service/blog/resource pages outweighs the benefit of cleaning a few orphaned rules. Orphaned CSS cleanup can happen in a future phase when service pages are also componentized.
</action>
<verify>
<automated>npm run build && npx playwright test e2e/homepage.spec.ts e2e/navigation.spec.ts e2e/blog.spec.ts --project chromium</automated>
</verify>
<acceptance_criteria> - src/pages/index.astro is under 120 lines total (frontmatter schemas + imports + template + script) - src/pages/index.astro contains `import HeroSection from` and all 10 section imports - src/pages/index.astro contains `<BlogSection count={3} />` - src/pages/index.astro does NOT contain `blogPosts` array (removed -- BlogSection uses content collection) - src/pages/index.astro does NOT contain `faqItems` array (removed -- FaqSection has its own) - src/pages/index.astro does NOT contain `<section class="hero">` or any inline section markup - src/pages/index.astro has NO `<style>` block (all CSS moved to components) - Section order in template: HeroSection, TrustBar, CredentialsSection, WhyUsSection, ServicesSection, ProcessSection, BlogSection, FaqSection, CtaSection, ContactSection - `npm run build` exits 0 - `npx playwright test e2e/homepage.spec.ts --project chromium` passes - `npx playwright test e2e/navigation.spec.ts --project chromium` passes - `npx playwright test e2e/blog.spec.ts --project chromium` passes (blog page still works) - `ls src/components/sections/ | wc -l` returns 10 (all section components exist)
</acceptance_criteria>
<done>index.astro is fully refactored to ~70 lines of pure component composition. All 10 sections are standalone components. BlogSection uses dynamic content collection. No inline section markup or section-specific CSS remains in index.astro. All E2E tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` exits 0
2. `npx playwright test --project chromium` full suite passes
3. `wc -l src/pages/index.astro` is under 120 lines
4. `ls src/components/sections/*.astro | wc -l` returns 10
5. `grep -c 'getCollection' src/components/sections/BlogSection.astro` returns 1
6. `grep -c '<style>' src/pages/index.astro` returns 0 (no inline styles)
7. Homepage in browser renders all 10 sections in correct order
   </verification>

<success_criteria>

- index.astro is a clean composition file under 120 lines
- All 10 homepage sections are standalone Astro components
- BlogSection dynamically fetches latest 3 posts from content collection
- FaqSection contains its own FAQ data
- ContactSection wraps existing ContactForm
- All anchor link IDs preserved (services, blog, faq, contact, process)
- All E2E tests pass with no regressions
- global.css is not broken (no premature CSS removal)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-component-architecture-and-visual-redesign/03-03-SUMMARY.md`
</output>
