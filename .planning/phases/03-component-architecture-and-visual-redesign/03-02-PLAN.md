---
phase: 03-component-architecture-and-visual-redesign
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/sections/HeroSection.astro
  - src/components/sections/TrustBar.astro
  - src/components/sections/CredentialsSection.astro
  - src/components/sections/WhyUsSection.astro
  - src/components/sections/ServicesSection.astro
  - src/pages/index.astro
autonomous: true
requirements: [UIUX-01]

must_haves:
  truths:
    - 'HeroSection, TrustBar, CredentialsSection, WhyUsSection, ServicesSection exist as standalone Astro components'
    - 'index.astro imports and renders these 5 section components instead of inline markup'
    - 'Hero section markup renders identically to current site (same HTML structure, same classes)'
    - 'Credentials section uses imported CREDENTIALS data from src/data/credentials.ts'
    - 'Homepage still loads with correct heading and CTA visible'
  artifacts:
    - path: 'src/components/sections/HeroSection.astro'
      provides: 'Hero section with headline, CTA buttons, terminal animation'
      contains: 'class="hero"'
    - path: 'src/components/sections/TrustBar.astro'
      provides: 'Trust stats strip'
      contains: 'trust-stats'
    - path: 'src/components/sections/CredentialsSection.astro'
      provides: 'Credentials grid with OSCP, BlackHat, etc.'
      contains: 'cred-grid'
    - path: 'src/components/sections/WhyUsSection.astro'
      provides: '3-column why-us differentiators'
      contains: 'why-grid'
    - path: 'src/components/sections/ServicesSection.astro'
      provides: '3 service cards'
      contains: 'id="services"'
    - path: 'src/pages/index.astro'
      provides: 'Refactored homepage composing section components'
      contains: 'HeroSection'
  key_links:
    - from: 'src/pages/index.astro'
      to: 'src/components/sections/HeroSection.astro'
      via: 'import HeroSection'
      pattern: 'import HeroSection'
    - from: 'src/components/sections/CredentialsSection.astro'
      to: 'src/data/credentials.ts'
      via: 'import { CREDENTIALS }'
      pattern: 'CREDENTIALS'
---

<objective>
Extract the first 5 homepage sections (Hero, TrustBar, Credentials, WhyUs, Services) from the monolithic index.astro into standalone Astro components in `src/components/sections/`.

Purpose: This is the core architectural refactoring -- moving from a 1284-line monolithic page to composable section components. By extracting the top half of the page first, we establish the pattern and verify E2E tests still pass before extracting the rest.

Output: 5 new section component files, partially refactored index.astro that imports and renders these components instead of inline markup.
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-CONTEXT.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-RESEARCH.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-UI-SPEC.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: credentials data file -->
From src/data/credentials.ts:
```typescript
export const CREDENTIALS = [
  { badge: 'CERT', title: 'OSCP and OSCP+', org: 'Offensive Security' },
  { badge: 'CERT', title: 'PNPT', org: 'Practical Network Pentester' },
  { badge: 'TALK', title: 'BlackHat Europe Arsenal', org: 'Presenter, London 2023' },
  { badge: 'MSc', title: 'Computer Security', org: 'Technical University of Denmark' },
  { badge: 'BSc', title: 'Informatics', org: 'University of Athens' },
  { badge: 'OSS', title: 'Google Summer of Code', org: 'Honeynet Project 2023/2024' },
] as const;
```

<!-- From site.ts: existing exports -->

From src/data/site.ts:

```typescript
export const SITE = { name, legalName, url, locale, ... } as const;
export const CONTACT = { email, phone, whatsappUrl, ... } as const;
```

<!-- E2E test selectors that MUST be preserved -->

- `h1` (homepage heading) -- homepage.spec.ts line 11
- `link[name=/consultation|get in touch/i]` -- homepage.spec.ts line 17
- `#theme-toggle-btn` -- homepage.spec.ts line 24
- `nav` with links to services, blog, resources, contact -- navigation.spec.ts
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Extract HeroSection, TrustBar, and CredentialsSection components</name>
  <files>src/components/sections/HeroSection.astro, src/components/sections/TrustBar.astro, src/components/sections/CredentialsSection.astro</files>
  <read_first>
    - src/pages/index.astro (full file -- lines 336-412 for Hero+Trust, lines 575-627 for About/Credentials section, lines 758-899 for hero+trust CSS)
    - src/data/credentials.ts (created in Plan 01, CREDENTIALS export)
    - src/data/site.ts (CONTACT export for WhatsApp URL in hero)
    - src/styles/global.css (shared classes: .w, .sec, .sec-b, .sec-tag, .sec-title, .sec-sub, .btn, .btn-fill, .btn-wa, .btn-ghost, .btns, .reveal)
    - .planning/phases/03-component-architecture-and-visual-redesign/03-UI-SPEC.md (section order, component inventory)
  </read_first>
  <action>
Create the `src/components/sections/` directory if it does not exist.

**HeroSection.astro:** Extract lines 337-397 from index.astro (the `<!-- HERO -->` section) into a new file. The component:

- Imports `CONTACT` from `@/data/site` (for the WhatsApp URL in the hero button)
- Contains the full `<section class="hero">` block with: `.tag`, `h1`, `.hero-sub`, `.btns` (3 buttons: "Request a consultation" -> change to "Book a Consultation", WhatsApp, Our services), `.hero-terminal`
- IMPORTANT: Change the hero CTA text from "Request a consultation" to "Book a Consultation" per CONTEXT.md and UI-SPEC.md
- Has a `<style>` block containing ALL hero-specific CSS currently in index.astro lines 759-866 (`.hero` padding, `.tag`, `h1`, `.hero-sub`, `.hero-terminal`, `.terminal-*` styles)
- Also include the hero responsive overrides from index.astro lines 1240-1242 (mobile hero padding) and 1268-1276 (small hero padding, h1 max-width, hero-sub font-size)
- Uses the existing `.w` container class (NOT `.w-wide` -- hero content stays within 920px for readable line lengths; the hero section itself is full-width)

**TrustBar.astro:** Extract lines 399-412 from index.astro (the `<!-- TRUST -->` section). The component:

- No imports needed (pure static markup)
- Contains the `<div class="trust">` block with trust-stats
- Has a `<style>` block with trust CSS from index.astro lines 869-899 (`.trust`, `.trust-stats`, `.trust-stat`, `.trust-sep`)
- Include the light theme override: `[data-theme='light'] .trust { border-color: rgba(0, 0, 0, 0.07); }` from index.astro line 1199-1201

**CredentialsSection.astro:** Extract lines 575-627 from index.astro (the `<!-- ABOUT -->` section). RENAME the section concept from "About" to "Credentials" to match the UI-SPEC component inventory. The component:

- Imports `CREDENTIALS` from `@/data/credentials`
- Iterates over CREDENTIALS array to render the `.cred-grid` instead of hardcoded markup
- Contains the full section: `.sec-tag` ("ABOUT"), `.sec-title`, `.sec-sub`, `.stats-row` (4 stats), `.cred-label`, `.cred-grid`
- Has a `<style>` block with credentials CSS from index.astro lines 1036-1113 (`.stats-row`, `.stat`, `.cred-label`, `.cred-grid`, `.cred-card`, `.cred-badge`, `.cred-info`)
- Include light theme overrides from lines 1202-1207 (`.stat` and `.cred-card` border colors)
- Include responsive from lines 1252-1257 (`.stats-row` 1x1 on mobile, `.cred-grid` 1-col on mobile) and 1230-1232 (`.cred-grid` 2-col on tablet)
- Include small phone responsive from lines 1277-1282 (`.stats-row` 1-col, `.stat` padding)

All three components use existing global CSS classes (.w, .sec, .sec-b, .sec-tag, .sec-title, .sec-sub, .btn, .btn-fill, .btn-wa, .btn-ghost, .btns, .reveal) -- do NOT duplicate these in component styles. Only move section-SPECIFIC styles into component `<style>` blocks.
</action>
<verify>
<automated>ls src/components/sections/HeroSection.astro src/components/sections/TrustBar.astro src/components/sections/CredentialsSection.astro && grep -l "class=\"hero\"" src/components/sections/HeroSection.astro && grep -l "trust-stats" src/components/sections/TrustBar.astro && grep -l "CREDENTIALS" src/components/sections/CredentialsSection.astro</automated>
</verify>
<acceptance_criteria> - src/components/sections/HeroSection.astro exists and contains `<section class="hero">` - src/components/sections/HeroSection.astro contains `Book a Consultation` (not "Request a consultation") - src/components/sections/HeroSection.astro contains `import { CONTACT }` or `import { SITE, CONTACT }` - src/components/sections/HeroSection.astro contains `<style>` block with `.hero` padding rule - src/components/sections/HeroSection.astro contains `.terminal-cursor` animation style - src/components/sections/TrustBar.astro exists and contains `class="trust-stats"` - src/components/sections/TrustBar.astro contains `<style>` block with `.trust` styles - src/components/sections/CredentialsSection.astro exists and contains `import { CREDENTIALS }` - src/components/sections/CredentialsSection.astro contains `.cred-grid` in a `<style>` block - src/components/sections/CredentialsSection.astro iterates over CREDENTIALS (contains `.map(` or similar) - src/components/sections/CredentialsSection.astro contains light theme override `[data-theme='light']` - All three files contain responsive `@media` blocks
</acceptance_criteria>
<done>HeroSection, TrustBar, and CredentialsSection exist as standalone Astro components with scoped styles. CREDENTIALS data is consumed from the data file. Hero CTA says "Book a Consultation".</done>
</task>

<task type="auto">
  <name>Task 2: Extract WhyUsSection and ServicesSection, update index.astro to use all 5 components</name>
  <files>src/components/sections/WhyUsSection.astro, src/components/sections/ServicesSection.astro, src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (full file -- lines 414-475 for WhyUs, lines 477-538 for Services, lines 901-985 for CSS, full file for understanding what to keep in index.astro)
    - src/components/sections/HeroSection.astro (verify component pattern from Task 1)
    - src/components/sections/TrustBar.astro (verify component pattern from Task 1)
    - src/components/sections/CredentialsSection.astro (verify component pattern from Task 1)
    - src/styles/global.css (shared .card class at lines 360-410 -- this stays in global.css, NOT moved to ServicesSection)
    - e2e/homepage.spec.ts (selectors to preserve: h1, link with consultation/get in touch, #theme-toggle-btn)
    - e2e/navigation.spec.ts (selectors to preserve: nav links)
  </read_first>
  <action>
**WhyUsSection.astro:** Extract lines 414-475 from index.astro (the `<!-- WHY US -->` section). The component:
- No imports needed (pure static markup with inline SVGs)
- Contains the `<section class="why">` block with `.why-grid` and 3 `.why-item` entries
- Has a `<style>` block with why-us CSS from index.astro lines 901-943 (`.why`, `.why-grid`, `.why-item`, `.why-icon`, `.why-num`, `.why-item h3`, `.why-item p`)
- Include responsive from line 1233-1235 (`.why-grid` 1-col on tablet)
- Note: the `.why-grid` uses CSS Grid `repeat(3, 1fr)` on desktop and 1-col on mobile per tablet breakpoint

**ServicesSection.astro:** Extract lines 477-538 from index.astro (the `<!-- SERVICES -->` section). The component:

- No imports needed (all links are hardcoded service page URLs)
- Contains `<section id="services" class="sec sec-b">` with 3 `.card` elements
- Has a `<style>` block with ONLY the service-card-specific CSS: `.cards` grid from index.astro lines 946-950, `.card-tag` from lines 951-957, `.pills` from lines 958-972, `.card-cta` from lines 973-985
- IMPORTANT: The base `.card` class (lines 360-410 in global.css) stays in global.css because it's shared with other pages. Only add `.cards` (the grid container), `.card-tag`, `.pills`, and `.card-cta` to this component's scoped styles.
- Include responsive from lines 1218-1220 (`.cards` 2-col on tablet) and 1243-1245 (`.cards` 1-col on mobile)

**Update index.astro:** After creating the two new components, update index.astro to:

1. Add imports for all 5 section components at the top of the frontmatter block (after existing imports):

```astro
import HeroSection from '../components/sections/HeroSection.astro'; import TrustBar from
'../components/sections/TrustBar.astro'; import CredentialsSection from
'../components/sections/CredentialsSection.astro'; import WhyUsSection from
'../components/sections/WhyUsSection.astro'; import ServicesSection from
'../components/sections/ServicesSection.astro';
```

Use relative paths (matching existing import convention in this file), not `@/` aliases.

2. Replace the inline HTML for these 5 sections (lines 336-538) with component tags:

```astro
<HeroSection />
<TrustBar />
<CredentialsSection />
<WhyUsSection />
<ServicesSection />
```

NOTE: The section ORDER changes here per the UI-SPEC. Currently index.astro has: Hero -> Trust -> WhyUs -> Services -> Process -> About/Credentials -> Blog -> ... The new order moves Credentials to position 3 (after TrustBar, before WhyUs) per the locked decision "credentials must be visible before any service description."

3. Remove the corresponding CSS from the index.astro `<style>` block (lines 759-985): remove hero, trust, why-us, service cards, about/credentials CSS that was moved to components. KEEP the CSS for sections not yet extracted (process, blog-grid, CTA, contact, and responsive overrides for those remaining sections).

4. KEEP the following in index.astro -- do NOT remove:
   - The frontmatter (schemas, blogPosts, faqItems arrays) -- these will be moved in Plan 03
   - The `<!-- PROCESS -->`, `<!-- BLOG -->`, `<!-- FAQ -->`, `<!-- CTA -->`, `<!-- CONTACT -->` inline sections
   - The `<script>` block at the end (scroll-reveal, email-obfuscation)
   - CSS for remaining inline sections (process, blog-grid, cta-section, contact-grid, and their responsive overrides)

After this task, index.astro should have: imports at top, frontmatter data, BaseLayout wrapper, 5 component tags + 5 inline sections, script block, and reduced style block.
</action>
<verify>
<automated>npm run build && npx playwright test e2e/homepage.spec.ts e2e/navigation.spec.ts --project chromium</automated>
</verify>
<acceptance_criteria> - src/components/sections/WhyUsSection.astro exists and contains `class="why-grid"` - src/components/sections/WhyUsSection.astro contains `<style>` block with `.why` styles - src/components/sections/ServicesSection.astro exists and contains `id="services"` - src/components/sections/ServicesSection.astro contains `<style>` block with `.cards` grid styles - ServicesSection.astro does NOT contain `.card {` base styles (those stay in global.css) - src/pages/index.astro contains `import HeroSection from` - src/pages/index.astro contains `import ServicesSection from` - src/pages/index.astro contains `<HeroSection />` and `<TrustBar />` and `<CredentialsSection />` and `<WhyUsSection />` and `<ServicesSection />` - src/pages/index.astro section order is: HeroSection, TrustBar, CredentialsSection, WhyUsSection, ServicesSection (credentials BEFORE services) - src/pages/index.astro `<style>` block does NOT contain `.hero {` or `.trust {` or `.why-grid {` or `.cards {` (these moved to components) - src/pages/index.astro `<style>` block STILL contains `.process {` and `.contact-grid {` (not yet extracted) - `npm run build` exits 0 - `npx playwright test e2e/homepage.spec.ts --project chromium` passes (h1 visible, CTA visible, theme toggle works) - `npx playwright test e2e/navigation.spec.ts --project chromium` passes (nav links present, service pages load)
</acceptance_criteria>
<done>5 section components extracted and rendering. index.astro imports and composes them. Section order updated (credentials before services). Homepage builds and E2E tests pass. Remaining 5 sections still inline in index.astro for Plan 03.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` exits 0
2. `npx playwright test e2e/homepage.spec.ts e2e/navigation.spec.ts --project chromium` all pass
3. `ls src/components/sections/` shows 5 .astro files
4. `grep -c 'HeroSection\|TrustBar\|CredentialsSection\|WhyUsSection\|ServicesSection' src/pages/index.astro` shows at least 10 (5 imports + 5 usages)
5. Homepage in browser shows hero -> trust bar -> credentials -> why us -> services (credentials before services)
   </verification>

<success_criteria>

- 5 homepage sections are standalone Astro components in src/components/sections/
- index.astro delegates to named components for the top half of the page
- Section order places credentials before services per locked decision
- All E2E tests pass (no visual or structural regressions)
- Component CSS is scoped (not in global.css)
- Global shared CSS (.card, .btn, .sec, etc.) remains in global.css
  </success_criteria>

<output>
After completion, create `.planning/phases/03-component-architecture-and-visual-redesign/03-02-SUMMARY.md`
</output>
