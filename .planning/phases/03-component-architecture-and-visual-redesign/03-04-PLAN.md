---
phase: 03-component-architecture-and-visual-redesign
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/components/sections/HeroSection.astro
  - src/components/sections/TrustBar.astro
  - src/components/sections/CredentialsSection.astro
  - src/components/sections/WhyUsSection.astro
  - src/components/sections/ServicesSection.astro
  - src/components/sections/ProcessSection.astro
  - src/components/sections/BlogSection.astro
  - src/components/sections/FaqSection.astro
  - src/components/sections/CtaSection.astro
  - src/components/sections/ContactSection.astro
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/WhatsAppFloat.astro
  - e2e/cta-visibility.spec.ts
  - e2e/responsive.spec.ts
autonomous: false
requirements: [UIUX-01, UIUX-02, UIUX-03]

must_haves:
  truths:
    - 'Site presents a premium, professional aesthetic with refined typography and spacing'
    - 'Book a Consultation CTA is visible above the fold at 375px, 768px, and 1440px'
    - 'Layout renders correctly at 375px, 768px, and 1440px without horizontal overflow'
    - 'Both dark and light themes render correctly with no contrast issues'
    - 'Scroll reveal animations respect prefers-reduced-motion'
    - 'WhatsApp button is 56px on mobile with sr-only label'
    - 'Touch targets meet 44px minimum on all interactive elements'
    - 'All E2E tests pass including the activated CTA and responsive tests'
  artifacts:
    - path: 'src/styles/global.css'
      provides: 'Refined typography weights, updated font loading'
      contains: '--space-1'
    - path: 'src/layouts/BaseLayout.astro'
      provides: 'Font weights optimized (only 400 and 700)'
      contains: 'wght@400;700'
    - path: 'e2e/cta-visibility.spec.ts'
      provides: 'Activated CTA visibility tests'
      contains: 'test('
    - path: 'e2e/responsive.spec.ts'
      provides: 'Activated responsive layout tests'
      contains: 'test('
  key_links:
    - from: 'e2e/cta-visibility.spec.ts'
      to: 'src/components/Nav.astro'
      via: 'link role assertion'
      pattern: 'book a consultation'
    - from: 'e2e/responsive.spec.ts'
      to: 'src/pages/index.astro'
      via: 'viewport test'
      pattern: 'scrollWidth'
---

<objective>
Apply visual polish refinements across all section components, optimize font loading, ensure responsive layout correctness, activate E2E tests, and perform human visual verification.

Purpose: This plan transforms the mechanically-extracted components (Plans 02-03) into a polished, premium visual experience. Typography weights are consolidated, spacing is refined using the design token scale, light theme is verified, and automated tests confirm CTA visibility and responsive behavior. Human verification catches subjective aesthetic issues that automation cannot.

Output: Visually polished components, optimized font loading, passing E2E tests, human-verified premium aesthetic.
</objective>

<execution_context>
@/Users/aristofanischionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aristofanischionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-CONTEXT.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-RESEARCH.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-UI-SPEC.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-01-SUMMARY.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-02-SUMMARY.md
@.planning/phases/03-component-architecture-and-visual-redesign/03-03-SUMMARY.md

<interfaces>
<!-- UI-SPEC Typography Contract -->
| Role      | Size                         | Weight | Line Height | Font Family       |
|-----------|------------------------------|--------|-------------|-------------------|
| Label     | 12px (0.75rem)               | 400    | 1.4         | JetBrains Mono    |
| Body      | 15px (0.938rem)              | 400    | 1.7         | Plus Jakarta Sans |
| Heading 3 | 20px (1.25rem)               | 700    | 1.3         | Plus Jakarta Sans |
| Heading 2 | clamp(1.4rem, 2.4vw, 1.8rem) | 700   | 1.2         | Plus Jakarta Sans |

Font weights loaded:

- Plus Jakarta Sans: 400, 700 (NOT 500, 600, 800)
- JetBrains Mono: 400 (NOT 500)

<!-- UI-SPEC Spacing Scale -->

--space-1: 4px, --space-2: 8px, --space-4: 16px, --space-6: 24px, --space-8: 32px, --space-12: 48px, --space-16: 64px

<!-- UI-SPEC Above-the-Fold Contract -->

On 375x667 mobile: tag, h1, credential badges (compact), CTA buttons visible without scrolling
Terminal animation below the fold on mobile

<!-- Current Google Fonts link in BaseLayout.astro line 93 -->

href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global visual polish -- font loading, body tokens, WhatsApp sizing, hero mobile ordering</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, src/components/WhatsAppFloat.astro, src/components/sections/HeroSection.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (Google Fonts link at line 93 -- current weights loaded)
    - src/styles/global.css (all font-weight usages, current body line-height, .wa-float styles, .hero-badge/.sec-tag font-size)
    - src/components/WhatsAppFloat.astro (current sizing, aria-label)
    - src/components/sections/HeroSection.astro (current h1 font-weight, hero-sub, terminal, mobile responsive)
    - .planning/phases/03-component-architecture-and-visual-redesign/03-UI-SPEC.md (complete typography, spacing, color, motion contracts)
  </read_first>
  <action>
Apply these specific refinements to global infrastructure and the 3 non-section files:

**1. Font loading optimization (BaseLayout.astro):**
Change the Google Fonts URL from:

```
family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500
```

to:

```
family=Plus+Jakarta+Sans:wght@400;700&family=JetBrains+Mono:wght@400
```

This reduces font file downloads from 5 weights to 2 per UI-SPEC (only 400 and 700 for Plus Jakarta Sans, only 400 for JetBrains Mono).

**2. Body line-height (global.css):**
Change `line-height: 1.6;` to `line-height: 1.7;` in the `body` rule (line ~69). This matches UI-SPEC body line-height of 1.7.

**3. Font-weight consolidation in global.css ONLY:**
Search global.css for `font-weight` values. Replace according to UI-SPEC:

- `font-weight: 500` -> `font-weight: 400`
- `font-weight: 600` -> `font-weight: 700`
- `font-weight: 800` -> `font-weight: 700`
- `font-weight: 400` stays as 400
- `font-weight: 700` stays as 700

Specific locations in global.css to check and update:

- `.nav-links a` has `font-weight: 600` -> change to 700
- `.sec-title` has `font-weight: 800` -> change to 700
- `.card h3` has `font-weight: 800` -> change to 700
- `.blog-card h2` has `font-weight: 800` -> change to 700
- `.faq summary` has `font-weight: 700` -> keep as 700
- `.hero-badge, .sec-tag` has `font-weight: 500` -> change to 400

**4. Section `.sec-tag` and `.hero-badge` typography alignment (global.css):**
Update `.hero-badge, .sec-tag` to use `font-size: var(--text-xs)` (0.75rem / 12px) instead of the current `0.64rem`. This aligns with the UI-SPEC Label role at 12px.

**5. WhatsApp float accessibility and sizing (WhatsAppFloat.astro + global.css):**
In `src/components/WhatsAppFloat.astro`:

- Ensure `aria-label="Chat on WhatsApp"` exists on the link element
- Ensure sr-only text "Chat on WhatsApp" exists inside the link for screen readers

In global.css, fix WhatsApp sizing (currently 56px default/48px small phone -- should be 48px desktop/56px mobile):

- Change `.wa-float` default to `width: 48px; height: 48px;`
- Change `.wa-float svg` default to `width: 24px; height: 24px;`
- Add a mobile override:
  ```css
  @media (max-width: 768px) {
    .wa-float {
      width: 56px;
      height: 56px;
    }
    .wa-float svg {
      width: 28px;
      height: 28px;
    }
  }
  ```
- Remove the 480px override that currently shrinks the button

**6. HeroSection mobile above-the-fold optimization (HeroSection.astro):**
Add a mobile-specific reorder so that on screens <= 768px, the terminal animation is pushed below the CTA buttons:

- On mobile (max-width: 768px), set `.hero-terminal { order: 10; }` to push it to the bottom
- This ensures: tag -> h1 -> hero-sub -> btns -> terminal (on mobile)
- Also update HeroSection.astro's font-weight values: `h1` from 800 -> 700, `.tag` from 500 -> 400
  </action>
  <verify>
  <automated>npm run build && grep -c "font-weight: 800\|font-weight: 600\|font-weight: 500" src/styles/global.css src/components/sections/HeroSection.astro</automated>
  </verify>
  <acceptance_criteria> - BaseLayout.astro Google Fonts URL contains `wght@400;700` for Plus Jakarta Sans (not 500, 600, 800) - BaseLayout.astro Google Fonts URL contains `JetBrains+Mono:wght@400` (not 500) - global.css body rule has `line-height: 1.7` - global.css has NO `font-weight: 800` or `font-weight: 600` or `font-weight: 500` - HeroSection.astro h1 has `font-weight: 700` (not 800) - HeroSection.astro `.tag` has `font-weight: 400` (not 500) - global.css `.wa-float` default is `width: 48px; height: 48px` - global.css has `@media (max-width: 768px)` block with `.wa-float { width: 56px; height: 56px; }` - WhatsAppFloat.astro contains `aria-label="Chat on WhatsApp"` - global.css `.hero-badge, .sec-tag` font-size is `0.75rem` or `var(--text-xs)` - `npm run build` exits 0
  </acceptance_criteria>
  <done>Global infrastructure polished: font loading optimized (2 weights), body line-height 1.7, global.css font-weights consolidated, WhatsApp correctly sized, HeroSection mobile ordering fixed, sec-tag typography aligned.</done>
  </task>

<task type="auto">
  <name>Task 2: Section component font-weight sweep -- consolidate all 9 remaining section components to 400/700</name>
  <files>src/components/sections/TrustBar.astro, src/components/sections/CredentialsSection.astro, src/components/sections/WhyUsSection.astro, src/components/sections/ServicesSection.astro, src/components/sections/ProcessSection.astro, src/components/sections/BlogSection.astro, src/components/sections/FaqSection.astro, src/components/sections/CtaSection.astro, src/components/sections/ContactSection.astro</files>
  <read_first>
    - All 9 section components listed in files (scan each for font-weight values)
    - .planning/phases/03-component-architecture-and-visual-redesign/03-UI-SPEC.md (typography contract: only 400 and 700)
  </read_first>
  <action>
This is a mechanical find-and-replace sweep across all section component style blocks. HeroSection was already handled in Task 1.

For each of the 9 remaining section components, scan the style block for `font-weight` values and apply these rules:

- `font-weight: 500` -> `font-weight: 400`
- `font-weight: 600` -> `font-weight: 700`
- `font-weight: 800` -> `font-weight: 700`
- `font-weight: 400` -> keep as-is
- `font-weight: 700` -> keep as-is

Specific known locations to update:

- **TrustBar.astro:** `.trust-stat strong` has `font-weight: 800` -> 700; `.trust-stat span` has `font-weight: 500` -> 400
- **CredentialsSection.astro:** `.cred-badge` has `font-weight: 700` -> keep; `.cred-info b` has `font-weight: 800` -> 700; `.stat strong` has `font-weight: 800` -> 700
- **WhyUsSection.astro:** `.why-item h3` has `font-weight: 800` -> 700
- **ServicesSection.astro:** `.card-cta` has `font-weight: 700` -> keep
- **ProcessSection.astro:** `.step h4` has `font-weight: 800` -> 700; `.step-num` has `font-weight: 700` -> keep
- **BlogSection.astro:** check for any font-weight in component styles, consolidate
- **FaqSection.astro:** check for any font-weight in component styles, consolidate
- **CtaSection.astro:** check for any font-weight in component styles, consolidate
- **ContactSection.astro:** check for any font-weight in component styles, consolidate

After the sweep, there must be ZERO occurrences of `font-weight: 500`, `font-weight: 600`, or `font-weight: 800` across ALL section component files and global.css.
</action>
<verify>
<automated>npm run build && grep -rc "font-weight: 800\|font-weight: 600\|font-weight: 500" src/components/sections/\*.astro src/styles/global.css | grep -v ":0$"</automated>
</verify>
<acceptance_criteria> - TrustBar.astro `.trust-stat strong` has `font-weight: 700` - TrustBar.astro `.trust-stat span` has `font-weight: 400` - CredentialsSection.astro `.cred-info b` has `font-weight: 700` - CredentialsSection.astro `.stat strong` has `font-weight: 700` - WhyUsSection.astro `.why-item h3` has `font-weight: 700` - ProcessSection.astro `.step h4` has `font-weight: 700` - grep returns all zeros for non-standard font-weights - `npm run build` exits 0 - `npx playwright test e2e/homepage.spec.ts e2e/navigation.spec.ts --project chromium` passes
</acceptance_criteria>
<done>All 10 section components and global.css use only font-weight 400 and 700. No non-standard font weights remain. Build passes and E2E tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Activate E2E tests, verify dark/light themes, run full suite</name>
  <files>e2e/cta-visibility.spec.ts, e2e/responsive.spec.ts</files>
  <read_first>
    - e2e/cta-visibility.spec.ts (current state with test.fixme stubs from Plan 01)
    - e2e/responsive.spec.ts (current state with test.fixme stubs from Plan 01)
    - e2e/homepage.spec.ts (existing theme toggle test pattern)
    - src/components/Nav.astro (verify "Book a Consultation" CTA exists in both desktop nav-links and mobile nav-cta-mobile)
    - .planning/phases/03-component-architecture-and-visual-redesign/03-VALIDATION.md (test specifications)
  </read_first>
  <action>
**Step 1: Activate CTA visibility tests**

In `e2e/cta-visibility.spec.ts`, change `test.fixme(` to `test(` for all tests. The CTA should now be visible on all pages at all viewports because:

- Nav has "Book a Consultation" in desktop nav-links (visible at 768px+ viewports)
- Nav has "Book a Consultation" as nav-cta-mobile (visible at less than 768px viewports)
- The nav is fixed position, so it's always above the fold

Adjust the test if needed: the CTA visibility check should look for the nav CTA (not a hero CTA that might be below fold). The `getByRole('link', { name: /book a consultation/i })` selector will find the nav CTA.

For the bounding box check (verifying above fold), adjust to check that at least one matching CTA is within the viewport height. Since there may be multiple "Book a Consultation" links (nav + hero), use `.first()` which gets the nav one (it appears first in DOM).

**Step 2: Activate responsive tests**

In `e2e/responsive.spec.ts`, change `test.fixme(` to `test(` for all tests. These test:

- No horizontal overflow (body scrollWidth <= viewport width)
- Hero heading visible
- Nav functional

These should all pass after the refactoring.

**Step 3: Run full test suite and fix any failures**

Run the complete Playwright suite: `npx playwright test --project chromium`

If any tests fail:

- For CTA visibility failures: verify the nav CTA link text matches regex, check if the element is hidden behind another element, check z-index
- For responsive failures: check for horizontal overflow caused by elements wider than viewport, check hero heading visibility
- For existing test failures: check that selectors still match (h1, nav links, theme toggle button ID)

Fix any test failures by adjusting either the component code or the test expectations (prefer fixing code -- tests represent requirements).
</action>
<verify>
<automated>npx playwright test --project chromium</automated>
</verify>
<acceptance_criteria> - e2e/cta-visibility.spec.ts contains `test(` (not `test.fixme(`) for all test cases - e2e/responsive.spec.ts contains `test(` (not `test.fixme(`) for all test cases - `npx playwright test e2e/cta-visibility.spec.ts --project chromium` exits 0 with all tests passing - `npx playwright test e2e/responsive.spec.ts --project chromium` exits 0 with all tests passing - `npx playwright test --project chromium` full suite exits 0 (all existing + new tests pass) - CTA test verifies "Book a Consultation" link visible above fold at 375px, 768px, 1440px - Responsive test verifies no horizontal overflow at 375px, 768px, 1440px
</acceptance_criteria>
<done>All E2E tests activated and passing. CTA visibility confirmed at all viewports on homepage, blog, and service pages. Responsive layout confirmed with no overflow. Full Playwright suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual verification of premium aesthetic and responsive layout</name>
  <files>none (verification only)</files>
  <action>
Human verifies the complete component architecture refactoring and visual redesign:
- 10 homepage section components extracted from monolithic index.astro
- index.astro reduced from 1284 lines to ~70 lines of component composition
- Section order updated: credentials appear before services
- "Book a Consultation" CTA visible in nav on all pages at all viewport sizes
- Typography consolidated to 2 weights (400, 700)
- Font loading optimized (fewer weight files)
- WhatsApp button larger on mobile (56px) per Rwanda mobile-first design
- Blog section now dynamically fetches latest 3 posts from content collection
- Scroll reveal animations respect prefers-reduced-motion
- All E2E tests passing (CTA visibility, responsive layout, existing tests)

Verification steps:

1. Start the dev server: `npm run dev`
2. Open http://localhost:4321/ in browser at 1440px wide

**Desktop (1440px) verification:**

- [ ] Homepage loads with premium aesthetic (not template-feel)
- [ ] Hero: heading, "Book a Consultation" CTA, and terminal are all visible
- [ ] Sections flow: Hero -> Trust Bar -> Credentials -> Why Us -> Services -> Process -> Blog (3 posts) -> FAQ -> CTA -> Contact
- [ ] Credentials section appears BEFORE services section
- [ ] Blog shows exactly 3 latest posts with "View All Posts" link
- [ ] All cards have subtle hover effects (lift + border glow)
- [ ] Nav shows "Book a Consultation" CTA button
- [ ] WhatsApp float visible bottom-right

**Mobile (375px) verification -- use browser DevTools responsive mode:**

- [ ] "Book a Consultation" button visible in nav WITHOUT opening hamburger menu
- [ ] Hero heading + CTA buttons visible without scrolling
- [ ] WhatsApp button is large (56px) and easy to tap
- [ ] No horizontal scrollbar at any point while scrolling
- [ ] Hamburger menu opens and shows all nav links
- [ ] All sections render correctly in single-column layout

**Theme verification:**

- [ ] Toggle to light mode -- verify all sections have readable contrast
- [ ] Cards, FAQ items, and credential badges have visible borders in light mode
- [ ] Toggle back to dark mode -- no visual artifacts

**Animation verification:**

- [ ] Scroll down -- sections fade in smoothly as they enter viewport
- [ ] In browser DevTools, enable "prefers-reduced-motion" -- verify no animations play

3. Check a service page (e.g., /services/penetration-testing/) at 375px:

- [ ] "Book a Consultation" CTA visible in nav
- [ ] Page renders correctly at mobile width
      </action>
      <verify>
      <automated>npx playwright test --project chromium</automated>
      </verify>
      <done>Type "approved" if visual quality meets premium standard, or describe specific issues to fix.</done>
      </task>

</tasks>

<verification>
After all tasks complete:

1. `npm run build` exits 0
2. `npx playwright test --project chromium` full suite passes (all tests green)
3. Human has visually verified premium aesthetic at 375px, 768px, and 1440px
4. Human has verified both dark and light themes
5. Human has verified CTA visibility on mobile without opening hamburger menu
6. `grep -c 'font-weight: 800\|font-weight: 600\|font-weight: 500' src/styles/global.css src/components/sections/*.astro` returns 0 (no non-standard weights)
   </verification>

<success_criteria>

- Premium visual aesthetic approved by human (UIUX-01)
- "Book a Consultation" CTA visible above fold on all pages at all viewports (UIUX-02)
- Responsive layout renders correctly at 375px, 768px, 1440px without breakage (UIUX-03)
- Both dark and light themes render correctly
- Typography uses only 2 weight values (400, 700)
- Font loading optimized (3 font files instead of 7)
- WhatsApp button properly sized for mobile
- All automated E2E tests pass
- Human visual verification approved
  </success_criteria>

<output>
After completion, create `.planning/phases/03-component-architecture-and-visual-redesign/03-04-SUMMARY.md`
</output>
